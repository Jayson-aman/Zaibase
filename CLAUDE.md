# Zaibase Group — 開発ガイド

## ⏰ リマインダー（要対応）

| 期限 | 内容 | 担当 |
|---|---|---|
| **2026年7月21日ごろ** | **弁護士確認完了後 → 全国マッチング手数料機能を実装する**（業務委託・請負専門、成約時数%）。確認先は川原総合法律事務所。確認事項は `docs/legal/lawyer-checklist.md` の①有料職業紹介。実装準備は完了済み（`jobMatchingEnabled: false` を `true` に変更するだけで開放可能）。 | Claude Code |
| **ahiru収益化＋弁護士確認後** | **弁護士法72条確認完了後 → 以下3機能を開放する。** Firestoreの `platformConfig/features` に `legalFeaturesEnabled: true` を設定するだけで有効化可能。①`⚖️ 許認可・法令チェック`（kyoninkaScreen・AI法的判断）②`🛡️ 信頼スコア`（complianceGateScreen・弁護士確認前β）③`Zaibase法律相談バナー`（弁護士紹介に該当する可能性・弁護士法72条⑤グループ割引）。確認事項は `docs/legal/lawyer-checklist.md` の③④⑤。財源はahiru（受験アプリ）の収益で賄う予定。 | Claude Code |
| **Stripe審査通過後** | **建設の課金を再開する。** Firestoreの `platformConfig/features` に `billingEnabled: true` を設定するだけで有料プラン申込・Stripe決済が開放される。現在は料金プランページ・Stripe画面への導線・`doStripeCheckout()` をすべて停止中。 | Claude Code |
| **弁護士確認後（経営支援ツール）** | **資金繰りシミュレーター・価格転嫁交渉サポートを開放する。** ①Firestoreの `platformConfig/features` に `cashFlowEnabled: true` / `priceNegotiationEnabled: true` を設定。②`priceNegotiationEnabled` は Claude API（`generateNegotiationLetter` Cloud Function）を使用するため、事前に `firebase functions:secrets:set ANTHROPIC_API_KEY` でシークレットを設定してからデプロイする。実装: `kensetsu/frontend/Zaibase.html`・`kensetsu/functions/management_tools.js`。 | Claude Code |
| **ahiru Web版の会員登録・課金に実際の需要が出てから（2026/9/8時点で保留）** | **ahiru Web版（exam.zaibase.group）で、公式集（¥50）・単元解放（¥100）の買い切りを開放する。** 現状RevenueCatのWeb Billingは消費型（買い切り）商品に対応していないため、`services/subscription.ts` の `fetchFormulaUnlockProduct`/`fetchUnitUnlockProduct` はWeb版では常に`null`を返し、ボタンは「準備中」表示のまま（iOS/Androidアプリ版は対応済み・問題なし）。開放するにはRevenueCatを介さず、Stripeへの直接ワンタイム決済（Checkout Session作成用Cloud Function＋決済完了Webhook）を新規実装する必要がある（体感数時間規模）。Web版のPro/Max月額課金自体もまだRevenueCat側のWeb Billing設定（Stripe連携＋`pro_monthly`/`max_monthly`パッケージ作成）が未完了で「準備中」のままなので、まずそちらを先に有効化すること。 | Claude Code |
| **ahiru：App Store Connect/RevenueCat側の導入オファー設定待ち（2026/9/11時点）** | **Pro・Max両方に「¥500・7日間・Pay Up Front」の導入オファーを追加する。** コード側（`components/Paywall.tsx`）は対応済み：RevenueCatが `product.introPrice` を返せば「まず¥500で7日間」を自動表示する作りにしてあるので、追加のコード変更は不要。残っているのは①App Store Connectで両サブスク商品に導入オファー（Pay Up Front・¥500・7日間）を作成、②RevenueCatのOffering設定に反映、の2つの管理画面操作のみ。無料トライアルではなく先払い¥500方式にしたのは、転換率に関係なく1人あたり確実に黒字（Max: 手数料引き後¥425−AI原価最悪値¥54=¥371、Pro: AI機能なしのため¥425まるごと）にするため。トライアル終了前日のプッシュ通知リマインドは「見送り」判断（`expo-notifications`未導入のため新規機能扱いになる）。 | Claude Code |
| **ahiru：全教科・全単元の公式集を少しずつ拡充する（進行中の継続タスク、2026/9/12時点）** | **今日と同じペース・同じ形式で、公式集（¥50買い切り）を科目・単元ごとに少しずつ広げていく。** 1回で全部やろうとせず、毎回1〜2分野ずつ進める運用方針で合意済み。<br>**完了済み**：①数学（koushiki系、`data/lessons-koushiki-k3souji/k4en1/k5en2/k6sanpei.ts`等）に相似・円・三平方の9項目（チェバ・メネラウス・外心内心垂心・二つの円・三平方と空間図形・面積比・体積比・影と砂時計・線分比と面積比）を追加、各項目は「解説＋動く図解＋マンガ解説（新規実装、`components/MangaDialogue.tsx`）＋証明形式の応用問題」の4点セット。②`app/(tabs)/formulas.tsx`（理科・算数・社会の無料公式集、既存62〜75項目）に新機能`FormulaItem.locked`（¥50買い切りロック、既存項目は無料のまま）を実装し、理科に生物7項目・地学7項目を追加。③図形に部分三角形を色分けする`PolyFigure.regions`をFigureViewに新規実装。<br>④理科の物理・化学の追加（2026/9/12）：`data/formulas-rika-butsuri-kagaku.ts`を新規作成し、熱の伝わり方・熱量計算・電熱線の発熱・豆電球の明るさ・組み合わせ滑車・ばねの直列並列・鏡の像・凸レンズの虚像（物理8）、水の三態・再結晶の計算・金属と水溶液・燃焼と重さ・気体の見分け方・中和の計算（化学6）の計14項目を追加。既存の無料項目と混ざらないよう別ファイルにし、`data/formulas.ts`で`理科: [...rikaFormulas, ...rikaButsuriKagakuFormulas]`と連結している（この方式なら既存ファイルを触らずに追加できるので、今後の追加も同じやり方でよい）。<br>⑤「その場で解ける一問一答」機能（2026/9/12完了）：`components/InlineQuiz.tsx`を新規実装。公式集（koushiki）側は`data/koushiki-questions.ts`でfigureId→問題の逆引きを作り、解説・図解・マンガの下に自動で表示（45公式・88問が全て紐づき済み）。ロック中の公式はLessonRendererの早期returnで止まるため問題は漏れない。`formulas.tsx`側は`FormulaItem.quiz`にその項目専用の問題を直接書く方式にし、算数62・理科73・社会81の全216項目に3問ずつ（計642問）追加済み。<br>⑥社会の追加・国語・英語の新規構築（2026/9/12完了）：公式・まとめを5教科に拡張した。社会は`data/formulas-shakai-tsuika.ts`に14項目（地形図・縮尺計算・地図記号・雨温図・輸送、文化史、土地制度、江戸の交通、選挙制度、民事刑事、裁判員制度、円高円安）。国語は`data/formulas-kokugo.ts`に25項目、英語は`data/formulas-eigo.ts`に23項目（中3範囲まで。仮定法・関係副詞・分詞構文は範囲外として除外）。新規教科の国語・英語は先頭3項目だけ無料、残りは`locked: true`。`data/formulas.ts`で`Subject`を5教科に拡張し、`app/(tabs)/formulas.tsx`の`SUBJ_ICON`とタブ余白（5教科でも狭い端末に収まるようアイコン16px・gap詰め）も対応済み。⚠️買い切りの識別キーは`item.label`なので、新規項目を足すときは全7ファイルを通して項目名が重複しないこと（確認コマンド：`grep -h "^        label: '" data/formulas-*.ts | sed "s/.*label: '//;s/',$//" | sort | uniq -d`）。<br>⑦通常の教科書単元への「関係する一問一答」（2026/9/13完了）：`data/lesson-questions.ts`を新規実装し、単元ページ（`app/lesson/[id].tsx`）に表示。単元は5,340あり問題側に単元タグが無い（15,000問の再タグ付けは非現実的）ため、単元タイトルから特徴語を取り出して問題文に含まれるかで拾う近似方式。要点は「話題を表さない語を落とす」ことで、①その教科の問題全体での出現数がしきい値（1.5%または12件）を超える語は不採用、②2文字の語は専門語（枕詞・約数）と一般語（太陽・秋田）が混ざるので5件以下のときだけ採用、③構造語（求め方・読み取り・しくみ等のリスト）・指示語始まり・英単語のみの語は不採用、④長文問題・200字超の問題は除外。全単元で測定してカバー率56%、抽出結果を目視で確認済み。見出しは「この単元に関係する一問一答」と、正確な紐づけではないことを断って表示している。公式集の単元はスキップ（そちらは正確な専用問題を出しているため）。しきい値を触るときは必ず再測定すること（緩めると無関係な問題が混ざる）。 | Claude Code |

> このリマインダーは完了したら削除する。

## ahiru：コミット前の総点検（2026/9/14 整備）

**教材データやタブ画面をさわったら、コミット前に必ず `cd ahiru && npm run audit` を通す。**
`scripts/audit.sh` が ①tsc ②データ監査（実行時）③コード監査（静的）④Webバンドル を順に実行する。
`✓` と `合計 0 件` かつ「すべて通過」で終われば良い。`ℹ`（要目視）は正当なものが混ざるので件数が増えたときだけ見る。

データ監査（`scripts/audit-data.ts`）は **esbuild で実際にデータ層を読みこんで** 検査する。
正規表現でソースを読むだけだと「ファイルは正しいのに index に結線されておらずアプリに出ていない」類の失敗を見逃すため、この方式は変えないこと。

### 過去に実際に起きた失敗と、その再発防止（監査が見る項目）

| 何が起きたか | 再発防止 |
|---|---|
| **既存単元と内容が重複した単元を16件書いた**（小4国語。「物語①場面の変わり目」が既存の「場面をとらえる」と同内容など） | **新しい単元を書く前に、必ずそのセル（受験種別×学年×教科）の既存単元一覧を出す。** 監査の「同一セル内のタイトル重複」が検出する。ただしタイトルが違えば通ってしまうので、事前の一覧確認が本体 |
| **書きかけの英単語が本文に残った**（`technique は使わない。ここでは`／`short く書きとめる`／`practice`／`女性を women` など。複数回発生） | 監査の「日本語の中に英単語が挟まっている」。図形の頂点（ABCD）・略語（PKO・GDP）・化学式（NaOH）・単位は除外済みなので、小文字の英単語が出たら本物 |
| **分数の `/` が罫線と判定され、算数66単元の計算式が等幅の箱に入っていた** | `LessonRenderer.isArtLine` に「等号と数字を含む行は計算式なので図あつかいしない」を追加済み。監査の「意図せず等幅の箱になる本文」 |
| **社会・理科だけ一覧が点滅していた**（`removeClippedSubviews` が画面外セルを切り離す／`FlatList` の data・renderItem が毎回別物） | 監査の「removeClippedSubviews を有効にしている」「FlatList を使う画面でメモ化が足りない」。`removeClippedSubviews` は使わない。data は `useMemo`、renderItem は `useCallback`、行は `React.memo` |
| **メモ化を足したとき、フックを早期returnより後ろに書いた**（tsc では検出できない） | 監査の「早期returnより後ろのフック」。フックはすべて早期returnより前にまとめる |
| **Web版（Mac）だけ、社会が点滅してスクロール位置が巻き戻った**。原因は2つ。①`imageSize = winWidth - 58` に上限が無く、Mac幅1440pxで640×640の画像を1382pxに引き伸ばしていた。さらに縦スクロールバーが出入りするだけで窓幅が十数px変わり、全行が別サイズで描き直されて高さが変わっていた。②Webでも仮想化していたため、セルが増えるたびに全体の高さが測り直され、ブラウザのスクロール位置が跳ねていた | ①窓幅からサイズを決めるときは `Math.min` で上限をつけ、`Math.round(x/32)*32` で丸める（監査の「useWindowDimensions を丸めずにサイズへ使用」）。②**Web版では仮想化しない**（`Platform.OS !== 'web' && …`）。ブラウザは画面外の要素を持っていても平気で、メモリが問題になるのはネイティブだけ |
| **「動く図解」が説明になっていなかった（全教科・全学年）**。`FigureView` のアニメーションは「SVGの部品を1.4秒かけて薄く重ねていく」だけで、動きも強調も式との対応もなく、図全体がぼんやり現れて終わっていた。しかも部品が何個あっても1.4秒固定だったため、部品の多い図では1個あたり0.1秒もなく、全部が同時に出たようにしか見えなかった。単元で使われる3,327図のうち2,617図（79%）が`steps`すら持たず、この無意味なフェードだけだった | `FigureView` を「1部品ずつ順に描き、描いたばかりの部品を強調色でひとまわり太くかさねる」方式に変更済み。描画時間も部品数に比例させる（1部品あたり約0.36秒、上限7秒）。イージングは外して等速にした（easeOutだと最初の数部品が一気に出る）。**図形データは1件も変えずに全図が改善する**作り。<br>**2026/9/15に続き**：問題集の図1,320枚のうち798枚（60%）は `steps` を持たず、線がすうっと現れて終わりで、何の図なのか・どの数がどこの長さなのかが文字では一切示されていなかった。`data/auto-steps.ts` を新設し、**図形データそのものから説明文を組み立てる**ようにして798枚すべてを埋めた（手書き522・自動798）。**手で書いた数値と図の数値が食いちがう事故が原理的に起きない**のが要点で、figure の中にある値しか読まない。`figure.steps` があれば必ずそちらが優先される。監査の「図解の説明／説明が出ない図」が0件を保つ |
| **公式・まとめタブの図解22件が、1件も `steps` を持っていなかった**（算数13・理科8・社会1）。単元ページ側の図解ばかり数えていて、**ユーザーが実際に毎日見ている公式・まとめ画面の図解を数えていなかった**。図解の網羅率を測るときは `lesson-figures` 側だけでなく、`FORMULAS` の各 `item.figure`（教科ファイルに直接書かれている）も必ず数えること | 2026/9/14に全22件へ7〜8段階の説明を追加済み。**網羅率の測定は esbuild でデータ層を読みこんで両方を走査する**（`FORMULAS` を回して `item.figure.steps` を見る）。ソースを grep するだけだと、書き方のちがう方をまるごと見落とす |
| **図解の説明に書く数値は、その図形データに入っている数値と必ず突き合わせる**。グラフ系（`lineChart`）は説明文だけ先に書くと図と食いちがう | ばね（10g→2cm）・溶解度（硝酸カリウム0℃13g・80℃169g、食塩35g→38g）のように、`points` の実データを読んでから文章の数値を決める |
| **一括置換の失敗で、別の文が文の途中にまるごと差しこまれて本文が壊れていた**（「二つの円の位置関係」④⑤。`④d= … r₁−r₂はr₁−r₂／<d<r₁+r₂のとき:2つの円は2点で交わる。…は(r₁≠r₂)のとき:` という読めない文になっていた）。**tsc も expo export も通るので、型検査では絶対に見つからない** | 監査の「同じ本文の中で40字以上がそっくり繰り返されている」。文単位で突き合わせる案は、差しこまれた側の前後がくっついて別の文になるため**実際には0件で効かなかった**。文の切れ目を無視して40字の窓でずらしながら比べること。正当な繰り返し（例文の再掲・国語の課題文の二度出し）が34件あるので ℹ 扱い、**件数が増えたときだけ中身を見る** |
| **一問一答の解説が、式をなぞるだけで「なぜそうなるか」を言っていない**（1,770問中1,221問＝69%）。「頂角40度の二等辺三角形の底角は」に「底角は2つとも等しいので (180−40)÷2＝70度」とだけ書いてあり、**なぜ底角が等しいのか**も、**140度が底角2つぶんの合計だ**という肝心なところも書いていなかった。ユーザーから「説明不足で、わからない人はわからない」と指摘された | **2026/9/15に全1,770問を書き終えて0件にし、監査を `info` から `check` に変えた。以後は1件でも出るとコミットが止まる。** 書くときの型：①公式が何を表しているか（面積なら「1cmの正方形が何個か」）②なぜその操作をするのか ③計算 ④よくあるまちがい。数だけ書いて終わりにしない。⚠️判定は検出語（なぜ・ので・ため…）によるので、理由が書けていても言い回しによっては拾われることがある。止まったら、まず本当に理由が書いてあるかを読んで確かめること。**検出語（`WHY_WORD`）を足して通すのは本末転倒なので、安易に広げない** |
| **一問一答に解説そのものが1文字も入っていない問題が165問あった**（社会・国語に集中）。答えだけが出て終わるので、まちがえた子は「なぜその答えなのか」を知る手がかりが画面のどこにも無かった。これは質の問題ではなく**欠けている**ので0にできる | **2026/9/15に0件にし、監査を `info`（残債）から `check` に変えた。以後は1件でも出るとコミットが止まる。** `quiz` に問題を足すときは `explanation` を必ず同時に書くこと。書き方は上の行と同じ型（①用語の意味 ②なぜそうなるか ③事実 ④まちがえやすい点）。**歴史・地理のように事実を述べる問題ほど理由が抜けやすい**ので、「なぜその年か」「なぜその土地か」を必ず一文入れる |
| **問題集の解説が、画面ではほとんど読めなかった**。解説は全15,199問すべてに入っていて中央値167字あるのに、無料ユーザーには `hint`（中央値34字）だけが**「📖 解説」という見出しで**表示されていた。ヒントの無い532問では `explanation.split('\n')[0]` を出していたため、1行目が「【解説】」という見出しの文字しか無い問題（50問）では画面に「【(1)の解説】」とだけ出て、裏の1,000字超の解説がまったく読めなかった。さらにアップグレード案内は `!question.hint` が条件だったため、ヒントのある14,667問では**くわしい解説が存在すること自体が知らされなかった**。ユーザーから「すべて解説がない。わかんないよーと言われないように」と指摘されて発覚 | **2026/9/14に方針変更：解説本文は無料で全文を出す。** Proが受け持つのは 図解つき・覚え方・ひっかけ注意・AI弱点コーチ・聞き流し。表示は `utils/explanation.ts` の `explanationText()` に一本化した（先頭の「【解説】」だけの行は見出しと二重になるので落とす）。監査の「問題集の解説が画面上で短すぎる（20字以下）」が、**画面に出る文そのもの**を同じ関数で測る。**データを見るだけでは絶対に気づけない種類の失敗なので、「データにあるか」ではなく「画面に出ているか」で測ること** |
| **小学生向けの問題に、中学以上でしか習わない内容が出ていた**。平方根（√）は中学3年で習い、中学受験の算数でも使わないのに、「1辺8cmの正方形の対角線は8√2cm」「正三角形の面積＝(√3/4)×一辺²」のような問題・解説が48問あった（算数42・理科6）。ユーザーから「小学生が平方根習いますか？」と聞かれて発覚 | 監査の「小学生向けの問題に平方根が出ている」。**問題文と答えだけ見ても取りこぼす**。解説・ヒント・覚え方（memoryTip）・ひっかけ注意（pitfall）まで全部の欄を見ること（実際、解説を直したあとに覚え方の欄へ「√の整理」が残っていた）。**書きかえの型**：正三角形・正六角形は高さや正三角形1個の面積を問題文で与える／対角線の長さは「面積」や「4等分した三角形」を問う形に変える／3:4:5・6:8:10・5:12:13 の整数比に当てはまるものはその比で読む／「2回かけて□になる数」という言い方にする。**小学生が解けない問題を出すのは困るが、コツの欄で「中学ではこう一発で解ける」と紹介するのは別で、こちらはむしろ良い** |
| **高校受験（中学生）向けの問題に、高校の内容が「入試に出る問題」として出ていた**（sin/cos/tan・微分積分・Σ・ベクトル・行列・log、計54問）。うち51問は `maxOnly`（Maxプラン限定）だったが、**`maxOnly` は課金の出し分けであって「先取り」という表示ではない**ので、生徒は入試範囲だと思ってしまう。`koko_max_sansu_35` の1次変換（行列）にいたっては、現在の高校課程にも無い | 監査の「中学生向けの問題に印なしで高校内容が出ている」。**全員に出るものは中学範囲に書き直す**（sin Aを問う代わりに面積や辺の比を問う）。**Maxプラン限定のものは消さず、問題文の先頭に【高校先取り】を付け、ひっかけ注意の欄に「高校入試には出ない／中学ではこう解く」と書く**。内容は価値があるので消さない。ただし「入試に出る」と誤解させないことが大事。**数え方の注意**：`sin`/`cos`/`log` は英単語（singing・cost・biology）に当たるので、教科をしぼり語の切れ目で判定しないと数字が数倍にふくらむ（最初の集計では79問・418問と出た） |
| **中学受験（小学生）向けの場合の数の問題に、高校で習う記号が使われていた**（`C(9,3)`・`₇C₂`・`8C3`・`5!`・`P(9,3)`・`nCr`、38問）。小学生はこの記号を習わないので、**式が読めず、解説が解説として働いていなかった。**さらに、記号にたよって書かれた3問は答えそのものがまちがっていた（`tokyo_hosei_max_01` の問2は168→224・問3は56→48、`nandai_sansu_07` は420→240、`nandai_sansu_10` は384→336）。うち2問は解説の中で「…の計算過程を再確認すると」「本問の設定で解答欄を384で示す」と、**自分で導けていないことを書いたまま出荷されていた** | 監査の「【問題集】中学受験の問題に高校で習う記号が出ている」。平方根のときとまったく同じ種類の失敗（小学生向けに中学以上の内容が出ていた）である。**書きかえの型**：①まず順番を考えてかけ算する ②選んだ個数の並べ方でわって重なりを消す、と言葉で書く。階乗は `5×4×3×2×1` とそのまま並べる。円順列は「1つ固定して残りを並べる」と言いかえる。<br>⚠️**座標の点（`A(0,4)`・`C(6,4)`）と図形の頂点（`A2B2C2`）は正当**なので弾いてはいけない。`C(…)` は後ろに `=` か `通り` が続くときだけ組み合わせとみなし、`2C2` のように前が英字のものは見ない。この2つを分けないと誤検出が出る。<br>⚠️**解説に「再確認すると」「わずかにずれることがあるが」のような逃げ道が書いてあったら、答えを必ず自分で計算し直す。**書いた本人が導けていない証拠であり、実際その3問はすべて答えがまちがっていた。<br>⚠️**検査を入れて0件になったら、わざと違反を1つ入れて検出されるか確かめる。**このとき、`esbuild` はデータをバンドルに焼きこむので、**ファイルを元に戻したら必ずバンドルを作り直す**こと（作り直さずに実行して「戻したのに1件出る」と一瞬あわてた） |
| **書いた本人が答えを出しきれていない解説が、そのまま出荷されていた**（6問）。解説の中に「…の計算過程を再確認すると」「本問の設定で解答欄を384で示す」「厳密には168通りは再確認が必要です」「条件再確認が必要だが」「出題ミスの可能性がありますが」という文が残っており、**その6問すべてで答えか問題文がまちがっていた。**（`nandai_sansu_07` 420→240、`nandai_sansu_10` 384→336、`tokyo_hosei_max_01` 問2 168→224・問3 56→48、`myojo_sansu_max01` 問4 1/3→5/14、`kankan_sansu_r01` 問3 8秒→8.75秒、`koko_nyushi_rika_16` 0.4g→0.3g）。問題文そのものが解けない形になっていたものも2件あった（`chugaku_sansu_ex_01_023` は追いかけの条件が速さと合わず、`koko_max_eigo_12` は「1語不要」なのに4語余っていた） | 監査の「【問題集】答えを出しきれていない解説が残っている」。**逃げ道を書いた時点で、書き手は導けていない。それは必ず中身のまちがいとして残る。**見つけたら、まず自分で最後まで計算し直し、答えと問題文の両方を確かめる。問題文が解けない形なら、**答えを合わせるのではなく問題文を直す**（追いかけの条件を3分→10分にして、もとの答え1920mと整合させた）。<br>⚠️**「前後で」「後で」「厳密には」「矛盾」を検出語に入れてはいけない。**質量保存の法則（反応前後で）・古文の「やがて＝後で ではない」・選択肢が本文と矛盾する説明など、正当な使い方が数百件ある（実際、広い検出語では261件出て、そのうち本物は5件だった）。**検出語は「書き手の迷い」を表す言い回しだけに限る。** |
| **解説は正しく導いているのに、答えの欄だけが古いまちがった値のままだった**（8問）。画面には答えの欄が出るので、子どもは**正しい解説を読んだうえで「答えが合わない」**という一番こわい状態になる。`koko_moshi_sansu_09` 2√10→4√5、同10 a=1/2→3/2、同39 5:4→13:10、同41 2500→3000円、同46 11→13、`term1_2026_chugaku_sansu_048` 3:7→5:11、`koko_oyo_sansu_42` 24→18cm³、`kankan_sansu_07` 3通り→2通り。しかも半数は解説の中で「※問題のヒント答え11は間違い、正しくは13」と**自分で気づいて書き置きしながら、欄を直していなかった** | 監査の「【問題集】答えの欄と解説の結びが食いちがう」と「答えを出しきれていない解説が残っている」の2つが見張る。**気づいたら、その場で答えの欄を直す。**本文に注意書きを足すのは直したことにならない。<br>⚠️**食いちがい検査は、対象をしぼらないと使い物にならない。**答えを2つ以上並べた問題（「体積：144cm³ 表面積：176cm²」など）は解説が1つ目だけを結ぶのがふつうなので、**答えが数ひとつだけの問題にしぼり、その数が解説の結びに出てこないときだけ**拾う。しぼる前は176件・しぼった後は0件（誤検出のみ26件→0件）。<br>⚠️**問題文そのものが解けない形になっていることもある。**`koko_hibiya_sansu_02` 問2は「正三角形ABCの辺BC上にAB＝BDとなる点D」＝DがCと重なる図で、解説が3回言い直したあげく答えが出ていなかった。**答えを合わせるのではなく、問題文を解ける形に直す。** |
| **外国語が8件、画面にそのまま出ていた。**ユーザーから「ロシア語とか英単語とか中国語とか、いろいろ混ざっているようですけど」と指摘されて全数を調べた。**中国語の簡体字2件**（「発音`记`号」＝正しくは記号、「燃焼`时`に」＝正しくは時に）、**ハングル1件**（「`친`しい友人」＝正しくは親しい）、**英単語5件**（「満`water` になる」「実験で `both`使われる」「もう一度`practice`する」「日本では `north` 側が下がる」「競争`relationship`にある」）。見逃していた理由は**検査の穴**で、①よその国の文字の検査は問題集しか見ておらず単元を見ていなかった ②英単語の検査は単元しか見ておらず問題集を見ていなかった ③**中国語は日本語と同じ漢字を使うので、文字体系で弾く方式ではまったく引っかからなかった** | 監査に「【単元】本文によその国の文字がまぎれている」「【問題集・単元】中国語の簡体字がまぎれている」「【問題集】日本語の中に英単語が挟まっている」の3つを追加した。**同じ種類の検査は、問題集と単元の両方に必ず当てる。**片方だけだと、もう片方が何年も素通りする。<br>⚠️**中国語の検出は、日本語にもある漢字を1字でも入れると使い物にならない。**最初のリストには 与（1618回）・写（1158回）・随（406回）・儿（部首名「ひとあし」として正当）がまじっていて、正しい本文を大量に誤検出した。**リストを足したら、必ずデータ全体での出現回数を数えて確かめる。何百回も出てくる字は、まちがいなく正しい日本語である。**<br>⚠️**英単語の検査は、しぼらないと本物が埋もれる。**大文字を含む語（SDGs・hPa・NaOH・RrYy・BtoC・AMa）と3文字以下（xcm・mol・gcd）は略語・記号・文字式で正当なので見ない。**小文字4文字以上だけ**を見ると、47件の雑音が2件まで減り、本物の5件がはっきり残った。外来語・ローマ字を教える単元は、英語を出すのが目的なので除く。 |
| **日本語そのものが壊れた文字が、画面に出ていた。**ユーザーから「日本語も合ってるか確認してくださいね」と言われて全81,159か所を調べた。**文字化け1件**（「相当範(U+FFFD)囲」＝範囲の字がこわれていた）、**半角カタカナ3件**（理科の表の中点が半角の `･`）、**かぎかっこの数が合わない3件**（`ee2_133` の問題文の先頭に余分な「、`koko_kiso_shakai_b_35` の覚え方が閉じ忘れ、`rika_s430` に余分な】）。**どれも tsc も expo export も通る**ので、型検査では絶対に見つからない | 監査に「【問題集・単元】文字化けした文字が画面に出ている」「半角カタカナが画面に出ている」を追加（どちらも合否に数える）、「かぎかっこ・すみつきかっこの数が合わない」は ℹ 扱いにした。<br>⚠️**丸かっこ（ ）の数は検査にしてはいけない。**座標 `A(2, 4)`・単位 `(㎠)`・箇条書き `a)` `1)` が入りまじるため、どう工夫しても誤検出が2,881〜24,246件出て使い物にならなかった。**本物の3件はすべて「」と【】で見つかった**ので、こちらだけを見る。<br>⚠️**「」の数が合わないのが正当な場合がある。**原稿用紙の使い方・作文・漢文の単元は、かぎかっこ**そのもの**を説明するので数が合わない（7件）。だから合否には数えない。<br>⚠️**送りがな（「行なう」）・ら抜き言葉（「見れる」）の検出は、国語の単元では誤検出になる。**それらを「まちがい」として教えている本文だからである（22件すべてが正当）。<br>⚠️**旧字体（慶應義塾・八幡製鐵所）・同じ漢字の3連続（表表表・富富富）も正当**なので弾かない。 |
| **古文の決まりと、歴史の年号にまちがいがあった。**ユーザーから「古文とかも確認してくださいね。日本語、数字 確認してください」と言われて調べた。**古文3件**：①`koko_kiso_kokugo_35` のヒントが「を→お（**語頭以外**）」となっていたが、正しくは「を→お（**助詞以外**）」。解説も「語頭の を は お」と書いていて、語中の「あを→あお」が説明できなかった。②`ke2_176` が「語の中の「を」だけが「お」に変わる」と書いていたが、例に挙げた をとこ・をり はどちらも語頭で、説明と例が食いちがっていた。③「いう→ゆう」の例が「**いふ→いう→ゆう**」になっていたが、動詞「言ふ」は「いう」で止まる（ゆうにはしない）。正しい例は きうり→きゅうり。**年号1件**：`koko_shakai_04_history_modern` に「サンフランシスコ平和条約＝1952年」とあり、**同じ単元の別の場所の「1951年署名」と食いちがっていた**（正しくは1951年署名・1952年発効） | 監査に「【問題集・単元】歴史の年号が出来事と食いちがう」を追加した。<br>⚠️**単元の `trapExamples`（`wrongAnswer`・`trapExplanation`）は、わざと誤った答えを見せて教える欄なので、事実の検査からは必ず外す。**外さないと「大化の改新（701年）」という**正当な誤答例**を拾ってしまう。<br>⚠️**年号の検査は「出来事の直後に、間に何も入れずに年号が書かれている形」だけを見る。**「794年に都を平城京から平安京に移した」のように年号が別の語にかかっている文まで拾うと、正しい本文が242件も誤検出される。<br>⚠️**古文・理科の数値の検査は、ほとんどが誤検出になる。**「エタノールの沸点は約78℃、水の沸点は100℃」を『水の沸点78℃』と誤認、「うつくしは現代の美しいとは違う」を『誤訳』と誤認、「ゐ・ゑ・を → い・え・お」の並びを『ゑ→い と書いている』と誤認した。**正しく教えている本文ほど、まちがいの語をならべて説明するので拾われやすい。**件数が出たら、まず原文を読む。 |
| **全5教科の「決まった事実」を全数照合した（20,863か所）。結果はまちがい0件だったが、判定を作る過程で分かったことが多い。**照合したのは、社会＝県庁所在地18組・世界の首都13か国・憲法の条文番号8つ・歴史の年号28件・地理の日本一、理科＝元素記号17・化学式9・人体/天体/分類の基本、算数＝面積と体積の公式7つ・解説中の計算式1,764個、国語＝古文の作品と作者12組・係り結び・古語の訳・四字熟語・ことわざ、英語＝不規則動詞29・比較級13・不規則な複数形14 | 監査に「【問題集・単元】教科の決まった事実と食いちがう」を追加した（県庁所在地・元素記号・化学式・不規則動詞・不規則な複数形・世界の首都）。わざと「愛知県の県庁所在地は横浜市」と書きかえて検出を確認済み。<br>⚠️**照合は「すぐ隣に書かれた組」だけを見る。**ゆるい条件では340件出て、**本物は0件**だった。「二酸化炭素（X）」の中の「炭素」、「亜鉛（Zn）」の中の「鉛」のように、**語の一部が別の語に含まれる**ことに必ず当たるので、直前が漢字・カタカナでないことを条件にする。<br>⚠️**英語の活用表は、区切りの前後に空白を必ず求める。**そうしないと `take-make-waste`（循環経済の用語）を「take の過去形が make」と読んでしまう。<br>⚠️**解説中の計算式の検算は66件出たが、すべて式の読み取り失敗だった**（比「1800:1400」を式と誤認、「20000×20000＝4億」の億を落とす、「513/27=19」の19を拾えないなど）。`scripts/verify-arithmetic.ts` の数字が出ても、**まず原文を読む**。<br>⚠️**穴うめの記号（酸素（Y））・問いかけ（〜はどこか）・外国の憲法（ワイマール第151条）・音の練習（big→bit→bid）は正当**なので、必ず除外する。 |
| **下書き段階で壊れた文字列・書きかけの英単語が混ざった**（ロシア語の単語が1件、`health を損なう` が1件、アポストロフィを避けようとして `Don not`／`my fathers father` という誤った英語が3件）。いずれも一括置換用のスクリプトを書いている途中で混入した | **スクリプトを走らせる前に、置きかえる文字列そのものを `grep` で点検する。** 日本語の解説に小文字の英単語が3文字以上続いていたら本物のミス（監査の「日本語の中に英単語が挟まっている」が拾う）。**TypeScript の文字列にアポストロフィが必要なときは、単引用符を避けるのではなく、二重引用符で囲んだファイル側に書く。** 英語をわざと崩して回避すると、英語の教材として誤りになる |
| **自動生成した説明が、図の使われ方とずれていた**。`coordinate`（座標平面）は理科でも「日食（地球・月・太陽）」「光の屈折」「太陽系の並び」のような**ただの模式図**として使われており、グラフとして説明すると「横がx軸」「直角三角形をつくる」というまったく的外れな文が出た。同様に、まるい池を表しただけの `circle`（植木算）に「二等辺三角形ができる」と助言していた | **図の `kind` は、その図が何を表しているかを保証しない。** 自動で文をつけるときは、中身を見て用途を判定してから書き分ける（`data/auto-steps.ts` の `isSchematic`：式のグラフも多角形も無く、点の名前が日本語なら模式図）。**生成した文は必ず教科ごとに実物を数件読む。** 件数が合っていることと、中身が正しいことは別 |
| **自動生成の文に `**強調**` を書いた**。`FigureView` はマークダウンを解釈しないので、画面に `**ならび順**` とアスタリスクがそのまま出る | 図解の `steps`・`caption` にマークダウン記法は使わない。`auto-steps.ts` の `keep()` が `**` を落とすようにしてある |
| **検出語による検査が、名詞に当たって素通りしていた**。「一問一答の解説が理由まで書けていない」の検出語に `くらべ|比べ` を入れていたため、割合の「**くらべる量** ＝ もとにする量 × 割合」や「柱状図を**比べる**」のように、**ふつうの名詞・動詞**として出てくる解説が「理由あり」と誤判定され、式だけの解説が9件通っていた | **検出語は「理由を述べる言い回し」だけに限る。** 内容語（比べる・考える・求める…）を入れると、その語を使う単元まるごとが素通りする。`くらべ|比べ` は2026/9/15に外し、9件すべてに理由を書き足した。**0件になった検査は、通っていること自体を疑って一度中身を読む。** 通し方が甘いだけかもしれない |
| **区別すべき2語を、同じ言葉づかいでまとめていた**。「氷がとける（融解）」と「食塩がとける（溶解）」が区別されず、公式集に「融解」が1件も無かった。ほかにも 蒸発／沸騰、領海／排他的経済水域、体積／容積、合同／相似 が、問題集には出ているのに公式集で並べて説明されていなかった | 監査の「区別すべき2語を、公式集で並べて説明していない（要目視）」。**問題集に出ているのに公式集で対にして説明していない語の組**を拾う。対の一覧（`CONFUSABLE`）に足していけば、同じ種類の見落としを機械的に見つけられる。**2026/9/15に対を31→177組に増やして再点検し、14件を追記して0件にした**（凝結／凝固、風化／しん食、弾劾裁判／国民審査、御成敗式目／武家諸法度、季節風／偏西風、一次関数／比例、有理数／無理数、可算／不可算 ほか）。⚠️**対の一覧には、受験範囲外の語や、対比の関係にない語を入れないこと。**毎回 ℹ に出続けて本当の見落としが埋もれる（加速度・定義域/値域・独立/排反は高校範囲、関係副詞は中3の範囲外で公式集から意図的に除いてあるため、11組を外した）。⚠️**公式集側の表記ゆれで誤検出する。**「侵食」は公式集では「しん食」と書かれていたため、検査側を実態に合わせた。**新しい検査を入れて0件になったら、わざと未対応の対を1つ加えて本当に検出されるか確かめてから信用する**（実際、`SUBJECTS` を添字に使う書きまちがいで全件素通り→全件検出という誤動作をしていた） |
| **使われていない「予備の解説」313件が、古い内容のまま残っていた**（`data/explanations_*.ts` 5教科）。問題側に解説が無いときのフォールバックとして配線されていたが、**実際に画面へ出るものが1件も無かった**。それだけなら無害だが、中身が問題の書きかえに追随しておらず、`sansu_42` は「36√2 ≈ 50.9cm³」という**誤った値**のまま残っていた（正しくは 18√2 ≈ 25.5cm³。ちょうど2倍）。問題側の解説が1件でも消えれば、この古い値が画面に出る | **使われていない予備データは、正しさを保てないので持たない。** 2026/9/15に配線ごと5ファイルを削除した。監査の「削除したはずの予備の解説ファイルが復活している」が見張る。**フォールバックを足すときは、「いま何件が実際に使われているか」を必ず数える。** 0件なら、それは安全網ではなく、更新されない誤りの置き場になる |
| **「0件・すべて通過」と報告していたが、見ていたのは公式集1,770問だけだった。** 子どもが毎日解く**問題集15,199問**（9倍）には「20字以下」という弱い検査しか当てておらず、そちらの残債をユーザーに伝えないまま完了報告を重ねていた。ユーザーから「少ししか出していないのに説明不足やミスが多い」と指摘されて発覚 | **完了を報告する前に、「いま測っているのは全体の何分の一か」を必ず言う。** 2026/9/15に問題集15,199問を全数点検し、監査に「【問題集】解説が理由まで書けていない」「【問題集】解説が60字以下」「【問題集】問題文も答えも同じ重複」を常設した。**この3つの数字を見ずに「すべて通過」と言わない** |
| **解説の計算を機械で検算したら、最初1,889件の食いちがいが出たが、ほとんどが自分の読み取り失敗だった**（√・²・π を落とすと数字だけが残る／「12×6÷2＝72÷2＝36」の途中式を分断／比の `1800:1400` の左右が混ざる／`5/6÷10/3` の分数／「200÷3＝66個」の切り捨て／「第1四分位数」の1を式と誤認／組み合わせ記号 `(9 3)`）。6回作り直して73件まで絞り、**残りも全部手で確認したところ、すべて読み取り失敗で中身のまちがいは無かった** | `scripts/verify-arithmetic.ts` に残した（除外の理由もすべてコメントに書いてある）。**件数が出ても、まず自分の読み取りを疑う。原文を読まずに直さない。** 日本語まじりの文から式を切り出すのは難しく、素朴な正規表現では誤検出が20倍以上出る |
| **解説の書き方に「短くまとめる」という誤った方針が残っていた**。ユーザーから「文字数や行数の制限にこだわらないでほしい。Geminiの解説のほうが断然わかりやすい。一見わかりやすいように見えてわかりにくいアプリになっている。実際にカスタマーが減っている」と指摘された | **解説に文字数の上限を設けない。** 短さより分かりやすさを優先する。書くときの型（この形で全問そろえる）：<br>①【何を聞かれているか】ひとことで言い直す<br>②【まず形をイメージする／なぜその式なのか】たとえ・図・公式の意味<br>③【ステップ1】【ステップ2】…と計算を1行ずつ分ける（途中の数を必ず書く）<br>④【答え】単位まで<br>⑤【確かめ】逆算や検算<br>⑥【よくあるまちがい】具体的に何をやりがちか<br>**「式を1行書いて終わり」は禁止。** ヒントが答えそのものになっているのも禁止（監査の「【問題集】ヒントが答えそのもの」が止める） |
| **小問（`subQuestions`）が問題集の画面にまったく出ていなかった**。89問・小問353問が該当。たとえば画面には「1辺が6cmの立方体の容器があります。1L＝1000cm³とします。」とだけ出て、**何を聞かれているのかがどこにも無い**。いっぽう答えの側には「問1 216cm³ / 問2 216cm² / 問3 3cm / 問4 6cm」と出るので、**見えない設問への答えだけが並ぶ**状態だった。`InlineQuiz`（公式集側）は描いていたが、`QuizCard`（問題集側）が描いていなかった | **データにある欄が、画面のどの経路でも描かれているかを確かめる。** 同じデータを2つの画面が使っていると、片方だけ実装されたまま気づかれない。2026/9/15に `QuizCard` で小問の設問・答え・解説を描くようにした。監査の「【問題集】小問に設問・答え・解説のどれかが無い」が中身の欠けを見張る。**「データにあるか」ではなく「画面に出ているか」で測る**（解説の hint 問題とまったく同じ種類の失敗） |
| **一括置換の道具を JSON から読むように変えたら、`\n` が本物の改行になって TypeScript の文字列が壊れた**。単引用符の文字列は改行をまたげないため `TS1002: Unterminated string literal` で `data/questions.ts` が壊れた | **改行は2文字の `\n` として書きこむ。** JSON から読むときは `body.replace('\n', '\\n')` を通す（`scripts` 外の作業用スクリプトに実装済み）。**壊れても `git checkout -- data/` ですぐ戻せるよう、書きこみ前に必ずコミット済みの状態にしておく**（今回はそれで1分で復旧できた）。`npx tsc --noEmit` を毎回通すこと |
| **解説の中に書いた `**強調**` が、画面にそのままアスタリスクとして出ていた（790行）**。`QuizCard`・`app/quiz/[subject].tsx` は解説を素の `<Text>` で描いており、マークダウンを解釈しない。「答えは 4√5」と見せたい所が「\*\*答えは 4√5\*\*」と表示されていた。図解の `steps` で同じ失敗をして `keep()` で対処したのに、**解説本文は見ていなかった** | 監査の「【問題集】画面に出る文にマークダウンの ** が混ざっている」。**画面に出る文（問題文・答え・ヒント・解説・覚え方・ひっかけ・小問）にマークダウン記法を書かない。**強調したいときは「かぎかっこ」か【見出し】を使う。**同じ種類の失敗を1か所で直したら、同じ記法を使う他の経路もその場で全部数える**（figureのstepsだけ直して解説を放置した） |
| **画面に出る文に、よその国の文字がまぎれこんでいた**。韓国語の文字が2件（理科の「케トン基」＝正しくは「ケトン基」、国語の「여明けの光」＝正しくは「夜明けの光」）、画面にそのまま表示されていた。下書き中にロシア語がまぎれた過去の失敗と同じ種類だが、**「日本語の中に英単語が挟まっている」の検査はラテン文字しか見ない**ので、キリル文字・ハングルは何年も素通りしていた | 監査の「【問題集】画面に出る文によその国の文字がまぎれている」。数学のギリシャ文字（α・π・θ）や上付き数字（²・³）は正当に使うので、**許す文字を並べるのではなく、受験教材にまず出てこない文字体系（キリル・ハングル・アラビア・ハングル・タイ・デーヴァナーガリーなど）を名指しで弾く。**許可制にすると、正当な記号が大量に誤検出されて使い物にならない（実際に11,411件出た）。**0件になった後、わざと `замен` を1件入れて検出されることを確かめてから信用した。** |
| **「問題文は中学範囲なのに、解説だけが高校の記号」という問題が113問あった。**既存の検査が**問題文と答えしか見ていなかった**ための抜け。たとえば「赤玉4個・白玉3個から2個取り出して同じ色になる確率」という完全に中学範囲の問題の解説に、`7C2 = (7×6)/(2×1) = 21` とだけ書いてあり、**中学生には式が読めず、解説が解説として働いていなかった**。内わけは 組み合わせ記号53・合同式（≡／mod）23・数学的帰納法8・フェルマーの小定理/中国剰余定理6・二項定理2・合成関数1。**うち20問は maxOnly ですらなく全員に出ていた**（答えはすべて正しく、記号だけが高校のものだった） | 監査の「中学生向けの問題の解説に、印なしで高校の記号が出ている」。**同じ種類の失敗の4回目**（小学生に√、中学生にsin/cos、中学受験に `nCr`、そして今回）。<br>**書きかえの型**（全員に出る20問はこれで中学範囲に直した）：「まず順番をつけてかけ算する（7×6＝42）→ 選んだ個数の並べ方でわって重なりを消す（42÷2＝21）」と言葉と式で書く。3個選ぶなら 3×2×1＝6 でわる。合同式は「3でわった余りが0のとき・1のとき・2のとき」と余りで場合分けして書く。<br>**maxOnly の93問**は消さず、問題文の先頭に【高校先取り】を付け、ひっかけ注意の先頭に「〜は高校で習う書き方で、高校入試には出ない。中学では〜」を書き足した。<br>⚠️**検査を全部の欄に広げてよいのは「中学でまず使わない書き方」だけ。** sin/cos を解説まで広げると118件出るが、その大半は「中学では三平方で解くが、高校ではsinを使うと一発」というコツの欄で、**これは誤りではなくむしろ良い説明**なので拾ってはいけない。<br>⚠️**座標の点 `C(5,14)` と化学反応式の `2Cu`・`2Cl` を必ず外す。**`C(…)` はうしろに `＝` か `通り` が続くときだけ組み合わせとみなし、`nCr` 型は「数字 C 数字」に限る（C のうしろが英字なら化学式）。これを分けないと誤検出が55件出る。 |
| **化学式の数字を全部「全角」で書いていたため、右上（価数）と右下（原子の数）が画面でまったく区別できなかった**（イオンの単元、490か所）。ユーザーから「記号の違いも確認してください」と言われて調べた。全角数字は上付き・下付きの区別が無く同じ高さに出るので、`SO４２−` は画面上「SO42−」としか読めず、**4が原子の数で2が価数だと読み取れない**。しかもこの単元は自分で「2の位置が前か右下かで意味がまったく変わる」「数字が前（係数）か右下（原子の数）かを必ず区別する」と教えており、**教えている区別を表記そのものが表せていなかった**。体積の単位も `cm３`（正しくは cm³）が125か所あった。**理科の他の12ファイルは元から `Cu²⁺`・`H₂O`・`SO₄²⁻` と正しく書いており、外れていたのはこの2ファイルだけだった** | 監査の「【問題集・単元】化学式や単位に全角の数字を使っている」（`[A-Za-z)][０-９]` の並びを見る。全数で誤検出0件）。単元本文・問題集だけでなく**図解のキャプションとラベルも見る**（同じ単元の図解は別ファイルにあり、本文だけ直すと図だけ古い書き方で残る）。<br>⚠️**数字が符号の直前にあっても「原子の数」のことがある。** `Cl₂⁻`（塩素分子が−1価という誤答例）・`H₂⁺`（水素分子という誤答例）・`NH₄⁺`（アンモニウム）・`NH₃⁺`（誤答例）・`NO₃⁻`（硝酸イオン）・`SO₄⁻`（誤答例）。「符号の直前の数字＝価数」で機械置換すると**これらが全部こわれる**。必ず原文を読んでから1つずつ決めること。<br>⚠️**「よくある誤り」としてわざと誤った書き方を見せている本文がある**（`誤り：Na¹⁺、Mg⁺²`／`OH²⁻ と書く……`／`CuCl₂ → Cu²⁺ ＋ Cl₂⁻`）。**誤りのまま残すのが正しい。**直すのは上付き・下付きの形だけ。最初に洗い出したとき、この種のものが「まちがい」として大量に出たが、読んだら全部が正当な教材だった。<br>⚠️**反応式のたし算の＋と、イオンの価数の＋を区別する。** `2Cl⁻→Cl₂＋2e⁻` の＋はたし算。図解ファイルでは空白なしで書かれているので、**符号を上付きにするのは「英字にくっついているとき」だけ**にする（数字にくっついた＋はたし算かもしれない）。<br>⚠️**図解ファイルには色コード（`#0EA5E9`）が入っている。**数字を機械的に置きかえると色が壊れるので、化学式の語をひとつずつ名指しで置きかえる。<br>⚠️**Python で `nxt in '＋−'` と書くと、`nxt` が空文字のとき True になる**（`'' in s` は常に True）。この罠で `CuCl２`→`CuCl²⁻` という誤変換が出た。**適用前に必ず差分をプレビューして読むこと。**これで変換器のバグを2つ（上記＋`NH４＋`→`NH⁴⁺`）とも適用前に見つけられた |
| **小5の問題に小6の内容が入っており、しかも小6側にまったく同じ問題があった。**ユーザーから「重複矛盾、中学高校の学年などの間違い」を確認してほしいと言われて見つけた。`grade_e5_sansu_47` が「直径20cmの円の面積」を出していたが、**円の面積は小6の内容**（小5は円周率と円周まで）。さらに `grade_e6_sansu_24` に同じ数値・同じ答えの問題がすでにあり、**学年ちがいと重複が同時に**起きていた。それを重複検査が素通りしていた原因は**単位の表記ゆれ**で、小5が「314cm²」・小6が「314㎠」と書かれていたため、文字列としては別物になっていた。組文字（㎠）は全体で291件あり、多数派の cm²（4,067件）から外れていた。組文字に直したあと、これまで隠れていた完全重複がもう1組（ひし形の面積）あらわれた | 監査の「【問題集】学年別の問題に、その学年より上で習う内容が出ている」と「【問題集・単元】単位に組文字（㎠など）を使っている」。<br>⚠️**単位の表記ゆれは、重複検査を静かに無効にする。**答えの文字列で突き合わせる検査は、`cm²`と`㎠`のように**見た目が同じで文字がちがう**ものに弱い。表記をそろえてから重複を測ること。<br>⚠️**学年の検査は算数の学年別セット（`grade_e*_sansu`）だけに当てる。**公式集（`koushiki_*`）は中学受験の発展内容を意図して先取りしているので、同じものさしで測ると正当な教材を大量に誤検出する（円の面積だけで20件、相似で16件出た）。国語の対義語「拡大 ⇔ 縮小」の解説が他教科とのつながりを紹介するのも正当。<br>⚠️**「相似」は中学受験では小6で正面から教える**ので、e6を対象に入れてはいけない。小5以下に出たら本物。<br>⚠️**答えが正しくても学年がちがうことがある。**この50問は計算も答えも全問正しく、まちがっていたのは「どの学年に置くか」だけだった。**答えの検算だけでは見つからない種類のまちがい。** |
| **答え合わせが「自己申告」だった。**答えを見たあとに自分で「✓正解 / ✗不正解」を押す作りで、**答えを見てから判断できてしまい、本当に解けたのかが分からなかった**。ユーザーから「正解と不正解を消して、答えを書くようにしてほしい。正解だったら花火。あとから採点・添削して、どこが間違えたか分かるようにしてほしい」と要望があった | `utils/grading.ts`（採点）＋ `components/AnswerInput.tsx`（答えを書く欄）を新設し、自己申告ボタンを廃止した。答え方は問題のつくりで3つに分かれる。**選択肢つき3,162問**はタップして自動判定、**短い答え7,725問**は書いて自動判定（どちらも正解なら花火）、**長い記述4,312問**は正誤をつけず模範解答と見くらべる添削にする。<br>⚠️**採点でいちばん大事なのは「合っているのに×にしない」こと。**書き方のちがいで×になると、正しく解けた子が自信をなくす。全角と半角、単位をつけるかどうか、「時速」の有無、上付き文字（cm²とcm2）、かっこの中の言いかえ（「カナダ（Canada）」はどちらでも正解）、「4分の3」と「3/4」、大文字小文字、文末の記号——これらはすべて正解にする。<br>⚠️**ただし「ちがう単位」を書いたら不正解にする。**単位を落として数だけで比べると、面積の問題に「50°」と答えたものが正解になってしまった（実際に起きた）。単位は「書かなくてよい」が「ちがうものを書いたら不正解」が正しい規則。<br>⚠️**判定は必ず全問で検証する。**`模範解答をそのまま入れたら必ず正解になるか`を7,725問で確認し、さらに書き方のゆれ5種類と、となりの問題の答えを入れる誤判定テスト1,097件を通した（誤って×0件・誤って○0件）。<br>⚠️**記述問題も無料枠を消費させること。**正誤をつけない経路を作ったとき、うっかり課金チェックごと素通りさせると記述4,312問が無限に無料になる。<br>⚠️**カードは自分で表裏の状態を持っている。**親で `revealed` を立ててもめくれないので、`forceReveal` を渡して開ける。答えを書く前にめくれないよう `lockFlip` も要る。 |
| **同じ種類の失敗の5回目。「解くのに高校の知識が要る」のに印がない問題が97問あった**（等差数列36・積分18・三角比18・判別式14・微分6・Σ3・ベクトル2）。うち11問は maxOnly ですらなく全員に出ていた。既存の2つの検査には穴があり、①片方は**問題文と答えしか見ない** ②もう片方は**見る記号が組み合わせ・合同式などに限られていた**。さらに、**LaTeX の書き方が画面にそのまま出ていた**（115か所）。`Σ_{k=1}^{n}k` は画面では「Σ_{k=1}^{n}k」としか出ず、生徒には読めない | 監査の「中学生向けの問題で、解くのに高校の知識が要るのに印がない」と「【問題集】画面に出る文に数式の書き方（LaTeX）が残っている」。<br>⚠️**見る欄は「解くのに必要な欄」＝問題文・答え・ヒントだけに限る。**解説やコツの欄まで広げると87問出るが、その大半は「中学では三平方で解くが、高校ではsinを使うと一発」という**正当な紹介**で、誤りではなくむしろ良い説明である。ここを分けないと検査が使い物にならない。<br>⚠️**LaTeX は `Σ[k=1〜n]`・`∫[a,b]`・`2^(n-1)`・`F(k+1)` にそろえる。**コーパスの他の場所ではすでにこの書き方をしている。マークダウンの `**` で同じ失敗をしているので**これは3回目**。画面に出る文に「表示のための記法」を書かない、と覚えること。<br>⚠️**印を付ける一括スクリプトは、オブジェクトの終わりを正しく見つけること。**ファイルの最後の問題で範囲が配列の外へはみ出し、`];` の後ろに `pitfall:` を書いて2ファイルを壊した（tsc が拾った）。同じインデントの `},` を終わりとみなすか、**壊れたら git checkout で戻して手で直す**ほうが速い。 |
| **書いて答える方式にしたとき、言いかえを×にしてしまう状態だった**（1,425問）。まったく同じ問題が別のファイルにあって答えの言い回しだけちがう組が21組見つかり（「平和主義」と「平和主義（戦争放棄）」、「一つの行動で二つの利益を得ること」と「一つの行動で二つの利益や目的を同時に達成すること」など）、これは矛盾ではないが、**きびしく1文字ちがいで判定すると、正しく分かっている子が×をもらう**。「月食」と書いた子が「月食。そのときの月は満月。」に対して×になる状態だった | `utils/grading.ts` の `judge` が、合わなかったときに模範解答が**文で書かれていれば**`review`（見くらべ）を返すようにした。○は今までどおり花火、×は数や用語の答えだけに出る。<br>⚠️**「文で書かれた答え」の見分け方**：句点がある／こと・ため・から で終わる／読点で3つ以上並べている／12字以上で助詞をはさんで語がつながっている。数から始まる答えは判定できるので対象外。<br>⚠️**判定を変えたら必ず全問で測り直す。**「模範解答をそのまま入れたら必ず正解」を7,725問、となりの問題の答えを入れる誤判定テストを2,553件（誤って正解0件）、数を1ずらして必ず不正解になるかを1,957問で確認した。 |
| **高校内容97問のうち、課金に関係なく全員に出る11問を中学範囲に書き直した。**印を付けるだけでは「入試に出ない内容を全員に出している」状態が残るため、記録してある方針（全員に出るものは中学範囲に書き直す）どおり作り直した。**11問のうち10問は答えがまったく変わらなかった**（残る1問も、重力加速度の書き方を中学理科の「100gで1N」に直したことによる 9.8N→10N のちがいだけ） | **中学範囲への書きかえの型**（今回使ったもの）：<br>・**等差数列5問** … 「等差数列」「一般項」「公差」「Sₙ」という用語と公式を捨て、「□番目 ＝ はじめの数 ＋ 増える数×（□−1）」「合計 ＝（はじめ＋おわり）× 個数 ÷ 2」と言葉で書く。**数を点、増えるところをそのあいだのすき間と考えると、点が20個ならすき間は19か所**と目で見てわかる（ここが唯一のつまずき所）。<br>・**三角比3問** … sin を使わず、30°・60°・90°の直角三角形の辺の比 1:2:√3 で解く。30°の斜面なら分力は重力のちょうど半分。平行四辺形の面積は垂線をおろして高さを出す。力の合成は平行四辺形（ひし形）の作図にもどし、正三角形2つぶんと見る。<br>・**判別式1問** … 「判別式D」を使わず平方完成する。`(x−1)²＝k+1` まで変形すれば、右辺が正・0・負のどれかで交点の個数が決まることが目で見てわかる。<br>・**積分・微分1問** … 放物線と直線で囲まれた面積は中学では出せないので、**問題のほうを等積変形（底辺が共通なら高さが同じで面積も同じ）に差しかえる**。接線を問う小問も、平行線と放物線の交点を問う形にすると中3で完結する。<br>⚠️**理科は単位のきまりも中学にそろえる。**「重力加速度 g＝9.8m/s²」は高校の書き方で、中学理科は「100gの物体にはたらく重力を1N」とする。ここを直すと答えの数値が変わることがあるので、**選択肢がある問題では選択肢も必ず直す**（今回は選択肢なしだったので影響なし）。<br>⚠️**書き直しても答えはほとんど変わらない。**高校の道具は速いだけで、中学の道具でも同じ答えにたどりつける。**答えが変わったら、書きかえをまちがえた疑いがある。** |
| **ahiru（中学・高校受験の教材）に、よその話がまざっていた。**ユーザーから「これは中学高校受験のためのものなので、建設や官公庁の話とは切り離してほしい」と言われて調べた。国語の敬語の単元に**「Zaibase の山田と申します。」**という例文があり、受験教材の中に自社サービス名が出ていた。敬語の例文に会社名を出す必要はまったくなく、学校名で十分である。<br>あわせて、残タスクを報告するときに**弁護士確認（Zaibase建設の話）をahiru の残タスクとして並べてしまった**。ユーザーから「建設会社に勤めている人間が、なんで中学高校受験をするんですか？」と指摘された | 監査の「【問題集・単元】教材に自社サービス名が出ている」（`Zaibase`・`RevenueCat`・`Stripe`・`Firebase`・`Vercel` などを弾く。わざと戻して検出を確認済み）。<br>⚠️**ahiru の話をするときに、建設・法律相談・官公庁の話を混ぜない。**プロダクトが別なら、残タスクの一覧も別にする。CLAUDE.md のリマインダー表には全プロダクトが並んでいるが、**ahiru の報告に写してはいけない**。<br>⚠️**「建設」「工事」という語そのものは弾かない。**社会科では建設業が第二次産業の例として正当に出てくる。弾くのは固有名詞だけにする。<br>⚠️**下請法・労働者派遣法・許認可は中3公民の正当な内容**（中小企業・労働・行政）なので、これも弾かない。 |
| **同じ種類の失敗の6回目。中学受験（小学生）向けの理科に、中学・高校の生物がそのまま出ていた（38問）。**ユーザーから「両方とも中学受験範囲に書き直してください」と言われて全数を調べた。内わけは**高校生物**（光補償点・光飽和点11問、原形質分離・浸透圧・根圧4問、明反応・暗反応・カルビン回路・NADPH3問、陽生植物・陰生植物2問、細胞呼吸の化学式・ミトコンドリア・ATP6問、斜面の分力にsinθ1問）と**中3の遺伝**（メンデル・遺伝子型・検定交雑・染色体・DNA・減数分裂・突然変異12問）。最初に見つけた21問を直そうとして検査を常設したところ、**さらに17問が出てきた**。小学生はこれらの語をまったく習わないので、解説が解説として働いていなかった。 | 監査の「【問題集】中学受験の理科に、中学・高校でしか習わない生物の用語が出ている」。わざと違反を入れて検出を確認済み。<br>**書きかえの型①（用語を落として言葉で書く）**：光合成のグラフ問題は、計算そのものは小学生でも解ける足し算・引き算なので、**用語だけを捨てれば中学受験範囲で完全に成立する**。「光補償点」→「光合成で使う二酸化炭素の量と、呼吸で出す量がちょうどつり合う明るさ」、「光飽和点」→「それ以上明るくしても光合成がふえなくなる明るさ」、「真の光合成量」→「実際に光合成で使った量 ＝ 見かけの量 ＋ 呼吸で出している量」。**答えの数値は1問も変わらなかった。**<br>**書きかえの型②（観察できる現象に置きかえる）**：原形質分離・浸透圧は用語も概念も範囲外だが、**同じ現象を身近な観察として書けば範囲内になる**。「高張液に浸すと原形質分離が起こる」→「キュウリに塩をふると水が出てしんなりする。水はうすいほうからこいほうへ移るから」。根圧・蒸散流も「葉から水が出た分だけ引き上げられる」と書けば中学受験範囲。<br>**書きかえの型③（言いかえがきかないものは差しかえる）**：遺伝は概念ごと中学受験範囲外で、言いかえようがない。この12問は**中学受験範囲の生物の問題に差しかえた**（ヒトのたんじょう・受粉と受精のちがい・保護色・けんび鏡の操作と倍率・食物連鎖のゆれ・こん虫のからだ・草食と肉食の見分け方など）。差しかえる前に、**そのトピックが既存問題とぶつからないかを必ず数える**（実際、肺ほう6件・じゅう毛6件は先に見つけて別の題材に変えた）。<br>⚠️**「葉緑体」を弾いてはいけない。**中学受験の理科でも「光合成は葉の葉緑体で行われる」と正面から教える語で、49問が正当に使っている。検出語に入れたら一気に50件出た。いっぽう**「ミトコンドリア」は中学2年の内容**なので弾く。**この線引きを誤ると検査が使い物にならない。**<br>⚠️**教科を rika にしぼる。**国語の論説文は iPS細胞・再生医療を題材にすることがあり、**課題文そのものに「生殖細胞」が出てくるのは正当**である（`kaimei_kokugo_1`）。<br>⚠️**F₁・F₂ を検出語に入れてはいけない。**力の合成の解説で2つの力を F₁・F₂ と書くのは正当で、遺伝とは無関係（`re_118`）。<br>⚠️**受験種別が未設定（`examType` なし）の問題がある。**`examType === 'chugaku'` でしぼると素通りする（`rika_42`・`rika-047`・`rika_11`・`rika_45` の4問がこれで一度見落とされた）。**`koko` 以外をすべて見る**こと。<br>⚠️**`figureDescription`（図の説明）も必ず見る。**問題文・答え・解説だけを見ていたため、`rika-047` のグラフの説明に「光補償点」「光飽和点」が残ったまま通りかけた。 |
| **同じ種類の失敗の7回目。高校受験の数学に、高校で習う統計・確率の用語が印なしで出ていた（15問）。**期待値5問・分散/標準偏差5問・正規分布1問・相関係数1問ほか。**うち3問は課金に関係なく全員に出ていた**（`koko_kankan_sansu_b_18` 標準偏差、`koko_kankan_sansu_c3_20` 期待値、`koko_sansu_ex_03_040` 分散）。期待値は高校数学A、分散・標準偏差・相関係数は数学Ⅰ、正規分布は数学Bで、いずれも高校入試には出ない。解説の残債を書いているときに1問見つけ、数え直して発覚した。 | 監査の「【問題集】高校受験の数学に、高校で習う統計の用語が印なしで出ている」。わざと印を消して検出を確認済み。<br>**書きかえの型**：**中学で散らばりを表すのは、範囲（最大値−最小値）と四分位範囲（第3四分位数−第1四分位数）で、箱ひげ図にまとめる。**標準偏差・分散を問う小問は、この2つに差しかえればそのまま中学範囲になる（データは変えなくてよい）。期待値は「5枚すべてを引いたときの合計 ÷ 5枚」と言いかえれば、**小学校で習う平均そのもの**になり、答えの数値も変わらない（140円）。⚠️このとき**はずれの0円も枚数に数える**ことを必ず書く。当たりの枚数でわると、くじが実際より当たりやすく見えてしまう。<br>⚠️**教科を sansu にしぼること。**「分散」は理科では光の分散（プリズム）、社会では工場の分散として正当に使われる。しぼらないと33件出て、本物は15件だった。<br>⚠️**覚え方・ひっかけ注意の欄まで見る。**問題文・答え・ヒント・解説を書き直したあと、`koko_kankan_sansu_c3_20` の覚え方に「期待値は金額×確率」が残ったまま通りかけた（平方根のときとまったく同じ取りこぼし）。<br>⚠️**印を付ける道具を走らせる前に、`pitfall` 欄があるかを数える。**無い問題ではかたまりの終わりに欄を挿入する経路に入り、過去に2ファイルを壊している。今回は先に手で欄を足してから走らせた（`koko_max_sansu_09`・`koko_meidai_max_01` の2問）。 |
| **下書きの点検を「忘れずに先に走らせる」運用にしていたため、1回すり抜けて `**強調**` を本番データに書きこんだ。**マークダウンを画面に出してしまう失敗は、図解の `steps`・解説本文・LaTeX と合わせてこれで5回目。毎回「次から気をつける」で終わらせていたのが原因で、手順に頼るかぎり必ずまた起きる。 | **手順ではなく道具側で止める。**`rw.py` / `rwf.py` が書きこみの前に必ず `pre.guard()` を呼び、問題があれば**1文字も書かずに中止**するようにした。わざとだめな下書きを流して、`git diff` が空のままであることを確認済み。<br>点検する項目は マークダウン（`**` `__` 見出し リンク バッククォート）・LaTeX・組文字の単位・よその国の文字・簡体字・日本語の中の英単語。<br>⚠️**英単語の検出は、日本語の直後だけを見ていると取りこぼす。**「、political にも経済的にも」のように**読点のうしろ**に紛れたものが素通りした（実際に書いてしまった）。読点・句点・かぎかっこの直後も見るように広げること。<br>⚠️**ただし英語の教材では英単語が正当に出る。**`rw.py 下書き.json eigo` のように引数を足したときだけ、英単語の検出を外せるようにしてある。一律に弾くと英語の解説が書けなくなる。<br>⚠️**それでも下書きに書いてしまうこと自体は減らない。**強調したいときは最初から「」か【】を使う、と決めておく。 |
| **方針転換：強調は `**太字**` で書いてよくなった（2026/9/16）。**ユーザーから「大切なところ・テストによく出るところは太字か赤字に」と指示された。それまでは ** が画面にそのまま出る失敗を4回くり返し、監査で禁止していたが、これは「描く部品が無かった」のが原因であって、強調そのものは教材に必要だった | `components/RichText.tsx` の `rich()` を作り、`**…**` を**太字の赤（#C0392B）**で描く。通しているのは 問題集の解説・ヒント・覚え方・ひっかけ・小問の解説（QuizCard／quiz画面／InlineQuiz）と、単元の本文・要点・ひっかけ例題（LessonRenderer／lesson画面）。<br>⚠️**問題文・答え・小問の設問と答えには ** を書かない。**rich() を通らないうえ、答えは採点（`utils/grading.ts`）の照合にそのまま使われるので、** が入ると正解が × になる。監査「採点や問題文に ** が入っている」が止める。<br>⚠️**監査と下書き点検（pre.py）は「閉じ忘れ（** の数が奇数）」を見張る。**奇数だとアスタリスクがそのまま画面に出る。<br>⚠️**図解の steps・title・description・intro・見出しは rich() を通らない**ので、そこでは使わない。<br>**書き方の型：単元には「★ ここがポイント：…」の行を必ず入れる**（LessonRenderer が ★ 始まりの行を枠で囲む）。その中の要となる語句を ** で強調する。1節に強調は2〜4か所まで。全部を赤くすると何も目立たない。 |
| **同じ種類の失敗の8回目。教科書（単元）側にも、小学生向けに平方根が残っていた（14単元・30か所）。**小4「約数」に「もとの数の平方根を超えたら終了」、小5「素数」に「√91≈9.5なので」、小6「最短距離」に「斜辺＝√(6×6＋8×8)」など。問題集の検査は2026/9/15に入れていたが、**単元を見ていなかった** | 監査の「【単元】小学生向けの単元に平方根が出ている」を追加。**同じ検査は問題集と単元の両方に必ず当てる**（外国語の検査でも同じ取りこぼしをした）。書きかえの型は問題集と同じ：「同じ数を2回かけてもとの数になる数」／3:4:5・5:12:13 の比で読む／相乗平均は「2回かけると両方の積になる数」。<br>⚠️**同じ文が本文とひっかけ欄の2か所にあることがある**（先頭の全角空白だけがちがう）。全部そろわなければ1文字も書かない方式で置きかえるときは、**順に置きかえた場合の一致回数**を数えること。 |
| **教科書（単元）の本文が、手順と例だけで「なぜそのきまりなのか」を言っていない（5,664件中1,328件）。**ユーザーから「問題を濃く書くなら教科書も同様で、同等以上の品質にすべき」「数学では検算方法も置いて」「教科書はここがポイントが品質」と指示された。小4の計算の順序に「かけ算が先」とは書いてあっても、**なぜ先なのか（かけ算はまとまりを表すから）**が書かれていなかった | 監査の「【単元】本文が理由まで書けていない（減らしていく数字）」（本文600字以下、または理由の語がない）。書きかえは `scratchpad/rwl.py`（既存の節は1文字も変えず、新しい節だけを差しこむ。バッククォートの中は区切りとみなさない）。<br>**足す節の型：①「なぜ〜なのか」…きまりの意味を図やたとえで（分配法則は長方形、かけ算は「まとまり」）②「確かめ（検算）のしかた」…算数は必ず。概算・一の位・両はしからかける・かっこをつけて書き直す、のように具体的な手順で。**どちらにも「★ ここがポイント：」の行を入れ、要の語句を ** で強調する。 |
| **単元の「ひっかけ」で正解と誤答が逆になっていた**（`sansu_s224` 仕切りのある水そう）。「左側から水を入れて水位15cmになるまで何分か」の正解を 3.9分（左を満たす2.4分＋全体で12→15cmの1.5分）としていたが、**仕切りをこえてあふれた水が右側を12cmまで満たす3.6分**（その間、水面は12cmで止まる）が抜けていた。正しくは 7.5分で、これは「最初から全体の底面積50cm²で 50×15÷100」という、もとの本文が"誤答"と決めつけていた計算そのもの（最終水位に必要な総量÷給水量なので、途中の段階に関係なく成り立つ） | 単元を深める作業で本文とひっかけを読み直すとき、**ひっかけの正解も自分で最後まで計算し直す**。「〜を見落とすと誤り」と本文が断じている計算が、実は正しいことがある。水そう・旅人算のように**段階が変わる問題は、段階を全部書き出してから足す**（左が満ちる→右が満ちる→全体で上がる）。監査は数値の正誤を判定できないので、読み直しが本体 |
| **中学受験の単元の図解と本文に、高校の記号・用語が残っていた**（`lesson-figs-sansu-ext07.ts` の caption/steps に `C(4,2)×C(4,2)＝36`・`C(6,3)＝20`・`C(5,3)＝10`・「余事象」、`lessons-sansu-ext07.ts` に「組み合わせC(チーム数,2)」、さらに同ファイルの図解に `3！＝6、4！＝24、5！＝120`・`(6－1)！` の階乗記号）。「中学受験の問題に高校で習う記号が出ている」の検査は**問題集だけ**を見ていて、単元本文・図解の caption・steps は見ていなかった。問題集で4回直した同じ種類の失敗が、単元側で素通りしていた | 2026/9/16に C(…)・余事象 は言葉に直した（「縦2本の選び方6通り×横2本の選び方6通り」「奇数5枚から3枚の選び方は10通り」「残りの84−10」）。階乗記号は 2026/9/16 に `lessons-sansu-ext07.ts`・`lesson-figs-sansu-ext07.ts` の全 70 か所を一括で直した（`n！`→`n×(n－1)×…×2×1`、`5！`→`5×4×3×2×1`、`(n－1)！`→「1人固定して残りを並べる」、`n！÷(a！×b！)`→「全部ちがうものとして並べた数 ÷ 同じものどうしの並べ方」、`余事象`→「反対の場合を全体から引く」、`nCr`→「組み合わせ」）。同日、`sansu_s278`「公差がマイナスの等差数列」が第10項＝−4 のように**負の数（中1）を前提**にしていたので、「6ずつ減る数列」（第8項＝50−42＝8、0を下回るのは第10項）に単元ごと書き直し、図解 `lf_ext08_03` の −4 の点も外した。**小学生向けの単元を深めるときは、−の値・マイナス・方程式・nCr・n！ が本文に無いかを先に読む**（task #12 の検査語に「マイナス」「負の数」「方程式」を含める）。⚠**一括置換の対象語（`5！`）を、同じスクリプトで書き足す新しい文（「高校では『5！』と書く」という紹介文）にも使っていたため、自分の新しい文まで置きかわって「高校では『5×4×3×2×1』と書く」という意味の通らない文になった。**置換の対象語を含む文を同時に足すときは、置換のあとに足すか、差分を読んで確かめる（実際に差分で見つけて直した）。**同じ種類の検査は問題集と単元（本文・図解・ひっかけ）の両方に当てる**（task #12 で `nCr`・`C(…)`・`n！`・余事象・順列/組合せ記号を単元側にも常設する）。図解の caption と steps は同じ文が2回書かれていることが多いので、`replace_all` で直す |
| **単元に足した「なぜ」「確かめ（検算）」の2つの節が、275単元すべてで逆順（確かめ→なぜ）に並んでいた。**節を差しこむ作業用スクリプト（`rwl.py`）が「末尾に足す」指定の節を、もとの最後の節の直後という同じ位置に順番に差しこんでいたため、後に書いた節が前に入りこんでいた。tsc も監査も通るので、**差しこんだ結果を目で読み直さない限り気づけない**。「確かめ」が「なぜ」の説明より先に出るので、読む子には根拠の無い検算が先に見える状態だった | 2026/9/16 に 275 組を機械的に入れかえ（「確かめ（検算）のしかた」の直後に「なぜ…」が来ている組だけを対象にし、テンプレート文字列のバッククォートの数で節の境目を読む）、`rwl.py` は同じ位置に複数の節を足すとき JSON に書いた順に並ぶよう直した。**一括で差しこんだ直後は、1件でよいので dump して節の見出しの順番を目で確かめる。**「差しこめた件数」が合っていることと、順番が正しいことは別 |
| **同じ種類の失敗の7回目。中学受験の理科の「単元」に、中2〜高校の生物・化学式がそのまま出ていた（31単元・18問）。**問題集側は前回（6回目）に検査を常設していたが、**単元側には当てていなかった**（「同じ種類の検査は問題集と単元の両方に当てる」の再発）。小4の「植物の細胞のつくり①〜③」が細胞壁・細胞膜・液胞・DNA・酢酸カーミン（中2〜中3）でまるごと書かれ、「光補償点」「光飽和点と陽生植物・陰生植物」は単元名ごと高校用語、「気孔の開閉」がカリウムイオン・アブシシン酸・CAM植物（高校）、総まとめ単元 rika_01・rika_06 が 6CO₂＋6H₂O→C₆H₁₂O₆＋6O₂・ミトコンドリア・オーキシン・メンデルの遺伝・反射弓・錐体細胞・インスリンで書かれていた。図解の caption にも「光補償点」「K+流入→浸透圧上昇」が残っていた。ついでに `rika_42`（カマキリの冬ごし）の `answerReading` が差しかえ前の「3対1。F1はすべて丸…」のままという**欄の直し忘れ**も見つかった | 監査に「【単元】中学受験の理科に、中学・高校でしか習わない生物の用語が出ている」を追加し、本文・要点・ひっかけ・**図解（caption・steps・軸ラベル）**まで見る。検出語に 細胞壁・細胞膜・液胞・DNA・クロロフィル・細胞小器官・酢酸カーミン・カリウムイオン・アブシシン酸・オーキシン・インスリン・グルコース・C₆H₁₂O₆ を足し、遺伝子型→遺伝子 に広げた（問題集側にも同じ語が効く）。<br>**書きかえの型**：①細胞の単元は「けんび鏡で見る植物のからだ（タマネギの皮とオオカナダモ）」「植物と動物のからだのちがい（養分のつくり方）」に差しかえ。②光補償点→「光合成と呼吸がつり合う明るさ」、光飽和点→「光を強くしても増えなくなる明るさ（頭打ち）」、陽生/陰生植物→「日なたを好む植物／日かげでも育つ植物」。③化学式は言葉の式「二酸化炭素＋水→（光）→デンプン＋酸素」に、グルコース→ブドウ糖。④気孔の開閉は「孔辺細胞が水を吸ってふくらむと外側のうすいかべがのびてそり返る」。⑤遺伝・ホルモン・神経系は言いかえがきかないので、「ヒトとメダカのたんじょう」「感覚器官（目・耳）」「体温の調節（あせ・血管）」に差しかえ。<br>⚠️ **グラフの「見かけの光合成量 −3.0」のような負の値は、小学生には「0より下＝放出」と言葉で読ませる。**軸ラベルを「二酸化炭素の出入り（0より上が吸収・下が放出）」にし、検算は「（−1.2）＋3.0」ではなく「3.0−1.2」で書く。<br>⚠️ 一括置換で `\n` を含む新しい文を単引用符の文字列に入れるときは、必ず2文字の `\n` に直す（`questions_rika_extra.ts` を一度壊して `git checkout` で戻した。過去の記録と同じ罠）。<br>⚠️ **問題を差しかえたら、`answerReading`（聞き流し用の読み上げ）まで全欄を直す。**画面に出ないので気づきにくい |
| **同じ種類の失敗の8回目。中学受験の理科に、中2〜高校の化学がそのまま出ていた（単元94・問題約90）。**光合成の化学反応式 6CO₂＋6H₂O→C₆H₁₂O₆＋6O₂、中和の HCl＋NaOH→NaCl＋H₂O、気体の発生の反応式（CaCO₃＋2HCl→…）、「化学反応式の係数をそろえる練習」という単元（小5）、総まとめ単元の「電気分解とイオン」、問題では イオン式・イオン化傾向・ボルタ電池・mol 計算・pH＝−log・周期表・原子の構造・核融合・活性化エネルギー まで、小学生向けとして出ていた。7回目（生物）と同じ日に、同じ理科で、分野を変えて見つかった | 監査に「【単元】【問題集】中学受験の理科に、中学・高校の化学の用語や化学式が出ている」を追加（化学式・化学反応式・元素記号・イオン・電気分解・mol・原子・還元・質量保存・周期表・核分裂・定比例・電離・電解質・イオン化傾向。図解の caption・equation・選択肢・読み上げ欄まで見る）。<br>**書きかえの型**：①化学反応式→**言葉の式**（「二酸化炭素＋水→（光）→デンプン＋酸素」「炭酸カルシウム（石灰石）＋塩酸→塩化カルシウム＋水＋二酸化炭素」）。約60種類の式は辞書で一括置換した（`scratchpad/chem_c.py`。＋/→/下付き数字/半角の表記ゆれを正規表現で吸収し、「二酸化炭素（CO₂）」のかっこ書きは消し、単独の記号は物質名にする）。②「質量保存の法則」→「重さの保存（結びつく前後で全体の重さは変わらない）」、「定比例の法則」→「重さの比はいつも同じ」。③イオンで説明していた中和→「酸の性質のもととアルカリの性質のもとが結びついて水になり、残りが塩」。④電気を通す水溶液→「とけているものが電気を運ぶ」（電離・電解質は使わない）。⑤言いかえがきかない問題（イオン化傾向・ボルタ電池・電気分解・mol・周期表・原子の構造・核融合・活性化エネルギー）は、金属と塩酸・備長炭電池・蒸発皿の残りもの・ミョウバンの溶解度・使い捨てカイロ・星の色と温度 など中学受験範囲の問題に差しかえた。<br>⚠️ **「ライオン」に「イオン」が含まれる。**検出語は `(?<!ラ)イオン`。最初の集計で s082・s100 が「イオン×15」と出たが全部ライオンだった。<br>⚠️ **「原子力」は社会でも使う正当な語**なので `原子(?!力)`。「分子」は算数の分数（分子・分母）があるので弾かない。粒の意味で使っていた所は「粒」に直した。<br>⚠️ **F₁・F₂（力の合成）・V₁（体積）は化学式ではない。**化学式の検出は「C・H・O・N・S＋下付き数字」と「2文字の元素記号＋下付き数字」に限る。<br>⚠️ **化学式のあとに数字が続くと（`CO₂44g`・`NaOH2.0g`）単語境界の判定で置きかえから漏れる。**辞書で一括したあと、必ず正規表現で再走査して残りを見る。<br>⚠️ **問題を差しかえたら questionReading・answerReading（読み上げ欄）・hint・memoryTip・pitfall・choices まで直す。**`rika_48` は本文を差しかえたのに読み上げ欄が「塩化銅水溶液を電気分解すると…」のままだった。<br>⚠️ 文字列を `'…' +` で連結しているファイル（takatsuki・toin_kindai）に、改行を `\n` に変換する道具を使うと壊れる。連結の行区切りは本物の改行なので、そのファイルだけは変換せずに書きこむ（`raw=True`） |
| **同じ種類の失敗の9回目。中学受験の理科に、中2〜高校の物理（電気・力学）がそのまま出ていた（単元約50・問題約120）。**電気の単元 s211〜s255 と総まとめ rika_02・rika_10 が、電圧（V）・Ω・オームの法則・電力（W）・ジュール・合成抵抗の逆数公式・電圧計・磁界・右ねじの法則・フレミングの左手/右手の法則・電磁誘導・変圧で書かれ、力学の総まとめ rika_05 が ニュートンの運動の法則・F＝ma・慣性・等速直線運動・作用反作用・仕事率（W）・位置/運動エネルギー・エネルギー保存・Pa で書かれていた。問題は「6Vの電池に4Ωと2Ωを並列」「Q＝I²Rt」「フレミングの左手の法則で親指は何を表すか」「質量5kgを3m持ち上げた仕事は何J」「自由落下 v＝gt」「向心力と人工衛星」まで、小学生向けとして出ていた。ついでに `nandai_rika_07`（音速）は解説が1002mと導いているのに答えが966m、`nandai_rika_02`（抵抗3個の組み合わせ）は解説の中で「5種類…4種類となる」と迷ったまま答えが「4R/3」を含む誤りだった。化学（8回目）と同じ日に、同じ理科で、分野を変えて3回目 | 監査に「【単元】【問題集】中学受験の理科に、中2〜高校の物理が出ている」を追加（`CHUGAKU_PHYS_NG`：Ω・オームの法則・数字＋V・ボルト・ワット・ジュール・電力・電圧・合成抵抗・逆数公式・磁界・磁場・フレミング・電磁誘導・誘導電流・右ねじ・起電力・加速度・ニュートン・運動/位置/力学的エネルギー・仕事率・慣性・等速直線・作用反作用・Pa・F＝ma・レンツ・スネル・屈折率。図解の caption・steps・選択肢・読み上げ欄まで見る）。わざと違反を2つ入れて検出されることを確認済み。<br>**書きかえの型（電気）**：**「電池1個・豆電球1個の電流を①、豆電球1個分の抵抗を1」**と決め、電流＝電池の数（直列）÷抵抗、直列は抵抗の足し算・電流はどこも同じ・発熱は抵抗の比、並列は枝ごとに「電池の数÷その枝の抵抗」・全体は合計・電流は抵抗の逆比・同じn本で全体の抵抗1/n（「電流を足してから電池の数÷電流でもどす」）、複合回路は「並列を先に1本にまとめる→直列に足す→電池の数÷抵抗→枝にもどす→合計で検算」の4STEP。明るさの順は電流の順、「何倍」は**電流×電流**（電池2個直列で4倍）。電熱線の発熱は電流×電流×抵抗、時間に比例、上昇温度は水の量に反比例。数値問題は「6V・3Ω」を「電池3個・抵抗3」に置きかえると答えの構造がそのまま保てる（答えは①や1/3のような比になる）。<br>**書きかえの型（磁石）**：磁界→「磁力のはたらく範囲」「磁力線（方位磁針のN極が指す向きをつないだ線）」、右ねじ→「導線の下の方位磁針は電流の向きに向かって左にふれ、上なら右。逆にすれば逆」、コイルの極は右手の法則（4本指＝電流、親指＝N極）、フレミングの左手→「モーターは電磁石と磁石の引き合い・しりぞけ合いで回り、整流子が半回転ごとに電流の向きを切りかえる。電池か磁石のどちらか1つを逆にすると逆回転、両方逆なら同じ向き」、電磁誘導→「磁石（またはコイル）を動かしているあいだだけ電流が流れる。近づける/遠ざける・N/Sで向きが逆、速く・巻き数多く・強い磁石で大きい」、変圧・交流→「発電所は燃料はちがっても磁石とコイルを回す」「LEDは少ない電気で長く光る」。<br>**書きかえの型（力学）**：仕事（J）・仕事率（W）→「道具を使っても力×動かす長さは同じ（仕事の原理）」を動滑車（半分・2倍）・輪軸（半径の逆比）・てこ（支点からの距離の逆比）・斜面（長さ÷高さ）で。エネルギー→「斜面を転がした球が木片を動かす距離は、球の重さに比例・放す高さに比例・速さ×速さに比例。斜面のかたむきは関係ない」「ふりこは放した高さまで上がる（くぎで長さが変わっても同じ）、周期は長さだけ（4倍で2倍）」。慣性の法則→「速さを変えるには力がいる。力がなければ同じ速さでまっすぐ進み続ける」。作用反作用→「押すと同じ大きさで押し返される（別の物体にはたらくのでつり合いではない）」「浮力の押し返しで台ばかりは浮力の分だけ重くなる」。N→「g・kg分の力」、Pa→「1cm²あたりにかかる重さ」、g＝9.8→使わない。<br>⚠️ **「逆数」は小6算数で習うので弾かない**（電流と抵抗は逆数の関係、と書いてよい）。弾くのは「逆数公式」「逆数の和」。<br>⚠️ **「明るさ2倍」と「発熱4倍」を同じ教材の中で混ぜない。**直前の型⑨は「明るさ＝電流」で明るさ2としていて、直前の型⑩の「明るさは2倍なのに発熱は4倍」と食いちがっていた（物理的にも誤り）。「明るさの順は電流の順、倍率は電流×電流」に統一した。同じ日に書いた「かん電池の直列は電池の持ちが同じ」（正しくは早く減る）も直した。<br>⚠️ **正規表現の置換文字列に `\n` を入れると本物の改行になり、単引用符の文字列が壊れる**（`re.sub(pat, new)` の `new` は置換テンプレートとして解釈される）。`lambda m: new` で渡す。2ファイルを壊して `git checkout` で戻した。<br>⚠️ **`'…' +` で連結しているファイル（otani・tezukayama・toin_v2 など）は、文の途中に `' +\n      '` が入るので、正規表現で文をまたぐ置換ができない。**欄ごと（`explanation:` の連結全体）を1つの文字列に置きかえる道具（`scratchpad/qlib.py` の `set_field`）を使う。<br>⚠️ **問題の一覧に出た id と、本文の位置がずれていることがある。**dump の区切りを見まちがえ、「関関同立(20)」を `kankan_top_rika_077` だと思って置換し、実際は `kankan_top_rika_085` だった（`term1_053` と `osakajo_rika_16` も同じ）。置換前に `grep` で id を確かめる。 |
| 同名の単元が同じ学年に2件（社会「日本国憲法の三大原則」） | 監査の「同一セル内のタイトル重複」。タイトルを内容に合わせて差別化して解消済み |
| 公式集の `label` は¥50買い切りの識別キーなので全教科で一意でなければならない | 監査の「公式集label重複」 |
| 新しい単元ファイルを作ったが index に `import`／`...spread` し忘れ | 監査の「単元ファイルの結線漏れ」 |

### 誤検出だったので、次は騒がない

- **「教科内のorder重複」は問題ではない。** 画面は 教科でfilter → orderでsort → 受験種別でfilter の順に処理するので、中学受験と高校受験が同じorder番号でも表示順は乱れない（実際に2,328件ある）。**同一受験種別の中で**重複していなければよい。
- **数値の食いちがい検査は誤検出が多い。** 間ノ岳3,190m を富士山の高さ、水中の音速1500m を空気中の340m、エタノールの沸点78℃ を水の沸点、参議院の「任期6年」を衆議院の任期、と誤って拾った。文脈を見ずに数値だけを比べないこと。
- 中学受験と高校受験で同名の単元があるのは正当（同じ話題を深さを変えて扱う）。同じ一覧に並ばない。

## Zaibase Group 全プロダクト一覧

| プロダクト | リポジトリ | URL | 担当ツール |
|---|---|---|---|
| Zaibase建設 | jayson-aman/zaibase (kensetsu/) | https://zaisai-share.web.app/app | Claude Code |
| Zaibase法律相談（Firebase版） | jayson-aman/zaibase (horitsu/) | https://zaibase-horitsu.web.app | Claude Code |
| Zaibase法律相談（Cloud Run版・本番） | 別リポジトリ（~/Documents/GitHub/Zaibase法律相談） | https://zaibase-legal-k56gkm3vfa-an.a.run.app | Cursor |
| 中学受験対策 ahiru | jayson-aman/zaibase (ahiru/)（旧 jayson-aman/ahiru は統合元として残存） | https://exam.zaibase.group | Claude Code |
| 宅建士対策 | jayson-aman/zaibase (takken/) | Vercel デプロイ予定（URL未定） | Claude Code |
| 高校受験 英単語・英熟語 EWP.Zaibase | ローカル・Git未管理（このリポジトリ未統合。Cursorで作成、RevenueCat課金は実機テスト済みで稼働中。英単語2,300語・英熟語2,200語、文法ワークブック全25章、ジョーク読解クイズ収録） | 未定 | Cursor |
| Zaibase Group 会社サイト | jayson-aman/zaibase.group | https://www.zaibase.group | Cursor |
| Zaibase証券（仮） | jayson-aman/miyabi-securities-app（公開・このリポジトリ未統合。旧 miyabi-securities はアーカイブ済み。Streamlit製AI金融分析ダッシュボード：ニュース・要人発言・軍事動向のAI分析でFX・先物・株式・暗号資産・新興テーマ株の値動きを予測。Python100%、Yahoo Finance/Google Newsがデータ元、教育目的・投資助言ではないと明記） | 未定 | Cursor |
| Zaibase物販（仮） | jayson-aman/eBay-export-profit-dashboard（公開・このリポジトリ未統合） | 未定（Streamlit Community Cloudでデプロイ） | Cursor |

## プロジェクト構成（このリポジトリ）

```
Zaibase/
├── kensetsu/          # Zaibase建設（Firebase: zaisai-share）
│   ├── frontend/Zaibase.html   # メインSPA（2万行超・単一ファイル）
│   ├── functions/              # Cloud Functions v2（27ファイル）
│   ├── firestore.rules
│   └── firebase.json
├── horitsu/           # Zaibase法律相談（Firebase: zaibase-horitsu）
│   ├── frontend/index.html     # 法律相談SPA
│   ├── functions/              # subscription.js / consult.js / revenue.js / invite.js
│   ├── firestore.rules
│   └── firebase.json
├── ahiru/             # 中学受験対策 ahiru（Expo/React Native、iOS・Android・Web）
│   ├── app/                     # expo-router 画面（タブ・クイズ等）
│   ├── components/ services/ hooks/ store/ data/
│   ├── services/subscription.ts # RevenueCat連携（Stripeではない）
│   └── vercel.json              # Web版デプロイ設定（Root Directory要設定）
├── takken/            # 宅建士対策（Expo/React Native、iOS・Android・Web）
│   ├── app/                     # expo-router 画面（ホーム・テキスト・問題集・マイページ）
│   ├── data/                    # 約1,400問＋全34章テキスト・図解（宅建業法・権利関係・法令制限・税その他）。令和7・8年本試験模擬50問×2含む
│   ├── services/subscription.ts # RevenueCat連携（プレースホルダー）
│   └── vercel.json              # Web版デプロイ設定（Root Directory: takken）
└── docs/
    ├── group/         # Zaibase Group 戦略・収益予測
    ├── kensetsu/      # 建設仕様書・設計画面
    └── horitsu/       # 法律相談仕様書
```

## 絶対に混ぜない

- **kensetsu（建設）** と **horitsu（法律相談）** は別プロダクト・別Firebase
- 法律関連コンテンツ → kensetsu に入れる（法律相談SPAではない）
- 決済・Auth・Firestore は各プロジェクト独立
- horitsu の `applyGroupDiscount`（建設プロプラン会員→法律相談¥1,000引き、`horitsu/functions/subscription.js`）は、弁護士法72条（周旋・紹介規制）の確認が取れるまで管理者承認（`groupDiscountGranted`）しない
- zaibase.group・法律相談（Cloud Run版）に「建設のトラブルはそのまま法律相談へ」等、建設と法律相談を商業的に誘導する文言を入れない
- 詳細は `docs/group/legal-compliance-addendum.md` 参照

## 法改正・税制改正の更新方針（宅建・法律相談・建設）

**方針：法改正・税制改正があった都度、該当コンテンツ（問題・解説・図解・テキスト）を最新の現行法に更新する。** 特に宅建（takken）は毎年10月の本試験が「その年の4月1日時点で施行されている法令」に基づくため、年度の法改正は必ず反映する。

- **更新対象**：takken の問題（`data/questions_*.ts`）・解説・章テキストと図解（`data/chapters.ts`）、horitsu/kensetsu の法律コンテンツ。
- **現在反映済みのベースライン（2026年時点）**：盛土規制法（宅地造成及び特定盛土等規制法・令和5年施行）、相続登記の申請義務化（令和6年4月）、成年年齢18歳（令和4年）、譲渡所得の長期20.315%／短期39.63%、不動産取得税 土地・住宅3%、固定資産税 小規模住宅用地1/6 等。
- **毎年チェックすべき典型項目**：税率・軽減措置の期限延長／改正（不動産取得税・固定資産税・登録免許税・印紙税の特例）、住宅ローン控除、各種特別控除の要件、都市計画法・建築基準法・宅建業法の改正、統計問題の最新データ。
- **作業手順**：法改正を反映する際は、①該当する問題・解説・図解を検索して更新、②`correctExplanation` と `choiceExplanations`・図解の数値を現行法に合わせる、③`recentChange` フィールドや `isRecentChange` があれば付与、④TypeScript型チェック後にコミット。

## ツール別担当まとめ

| 作業内容 | 使うツール |
|---|---|
| Zaibase建設・法律相談（Firebase）の機能追加 | **Claude Code（ここ）** |
| ahiru（受験）の機能追加 | **Claude Code（ここ）** |
| Cloud Run版法律相談の機能追加 | **Cursor** |
| 会社サイト（zaibase.group）の更新 | **Cursor** |
| 証券・物販（構築中）の機能追加 | **Cursor**（リポジトリ確定後にここへ追記） |
| デプロイ・GitHub管理 | **Claude Code（ここ）** |

## ahiru統合（2026/6/16 完了）

- jayson-aman/ahiru（公開リポジトリ）から `git clone` でコード取得 → `ahiru/` 配下にGit履歴なしで統合済み。秘密情報（`.env`等）は元リポジトリに含まれておらず、持ち込みなし。
- 旧 jayson-aman/ahiru リポジトリはこのまま残存（履歴保持用）。今後の開発はこのリポジトリの `ahiru/` でのみ行う。
- **課金はStripeではなくRevenueCat**（`ahiru/services/subscription.ts`）：iOS/Androidのネイティブアプリ内課金（App Store/Google Play IAP）をRevenueCat経由で利用する構成。`.env.example` のRevenueCatキーはまだプレースホルダーのため、本番キー（`EXPO_PUBLIC_RC_API_KEY_IOS` / `_ANDROID`）とApp Store Connect/Google Play Console側の商品設定が未着手。
- **Vercel連携：完了（2026/6/16）**：VercelプロジェクトのGitHub App権限を許可 → Connected Git Repositoryをこのリポジトリ（Jayson-aman/Zaibase）に変更 → Root Directoryを `ahiru` に設定 → `main` への統合コミット反映後、Production環境へのデプロイ成功を確認済み。
- **要確認**：Claude Code on the web に jayson-aman/ahiru 単体の別セッション（「受験」、Vercelデプロイ待ち状態）が残っている場合、この統合後は重複作業になるため終了・整理する。
- **現在の最優先タスク（2026/6/16時点・収益化優先）**：受験アプリのRevenueCat課金を有効化する。必要な3つのアカウント（RevenueCat／Apple Developer Program／Google Play Console）はいずれも未作成のため、Claude Codeがステップバイステップで作成・設定を案内中。コードは`services/subscription.ts`・`constants/proAccess.ts`に実装済みでプレースホルダーキーの差し替えのみで動く想定。
- 残タスク：ログイン方式の検討、既存の建設・法律相談と同様の監視・自動化フローへの組み込み。

### ⚠️ ローカルクローンの罠：`~/ahiru` は別プロジェクト（QualiZ）（2026/8/23判明）

ユーザーのMacのホーム直下 `~/ahiru` は、このリポジトリではなく**無関係の別アプリ「QualiZ」**（Bundle ID `com.jaysonaman.qualiz`、Expoプロジェクト `@masaya.nanjo/qualiz`）のローカルクローン。`cd ahiru` だけで移動すると誤ってQualiZ側でビルド・提出してしまう事故が実際に発生した（2026/8/23、EAS submitがQualiZ宛に実行され失敗して発覚）。

- **正しいクローン**（すべて `jayson-aman/zaibase` を指す、`ahiru/app.json` の `bundleIdentifier` が `com.zaibase.exam`）：
  - `~/zaibase-repo`（作業に使っているメイン）
  - `~/Documents/GitHub/Zaibase`
  - `~/Zaibase`（同名の入れ子 `~/Zaibase/Zaibase` も存在するが古い重複クローンの可能性、混同注意）
- iOS ビルド・提出コマンドを案内する際は、必ず `cd ~/zaibase-repo/ahiru` のように絶対パスを明示すること。「`cd ahiru`」のような相対パスだけの指示は誤爆の原因になる。
- ビルド前に `grep bundleIdentifier ahiru/app.json` で `com.zaibase.exam` になっていることを確認する運用を徹底する。

### iOSビルド時の既知の落とし穴（2026/8/23、解決済み）

2026/8/23、iOSビルド・提出が3つの原因で何度も失敗し、同じやり取りを繰り返した。**`eas build --platform ios` が成功しビルド番号3で `.ipa` が生成されたことを確認済み**（buildNumber 1→2→3、最終的に成功）。次回同様の作業をするときは、まず以下を順にチェックすれば同じ堂々巡りは避けられるはず。

1. **正しいディレクトリか確認する**：`~/ahiru` は無関係の別アプリ「QualiZ」（`com.jaysonaman.qualiz`）のクローンなので絶対に使わない。必ず `cd ~/zaibase-repo/ahiru` のように絶対パスで指定し、ビルド開始直後の表示で `Bundle Identifier: com.zaibase.exam` / `Project: @masaya.nanjo/entrance-exam` になっているか確認してから進める。
2. **ビルド番号はEAS側で管理している（2026/9/13変更）**：`eas.json` を `appVersionSource: "remote"` にしたため、EASはもう `app.json` の `ios.buildNumber` を書き換えない。したがってビルド前の `git checkout -- ahiru/app.json` は不要になった。<br>それまでは `local` だったため、EASが書いたビルド番号を毎回 `git checkout` で捨てる運用になっており、次のビルドでも必ず1→2と採番されて**同じ番号で二度提出しようとして失敗する**事故が実際に起きた（1.2.9(2)で発生）。`remote` ではEASが採番を覚えているので、この衝突は起きない。バージョン（1.2.10など）を上げるときだけ `app.json` を編集する。
3. **reanimated / worklets のバージョンが固定されているか確認する**：`ahiru/package.json` はこの2つを範囲指定なしの完全固定にしてある（`react-native-reanimated: 4.5.3` / `react-native-worklets: 0.11.1`、検証済みの組み合わせ）。`package-lock.json` はこのリポジトリ全体の方針でコミットしていないため、どちらかを `^4.4.1` のようなレンジ指定に戻すと、EASビルドのたびに最新版へ自動で引き上がり、組み合わせがずれて `pod install` やXcodeビルドが失敗する（実際に発生：worklets 0.12.x で `executeSync` が `runSync` 系にリネームされて削除されており、reanimated 4.6.0がそれを呼び出そうとして `no member named 'executeSync' in 'worklets::WorkletRuntime'` で失敗した）。**この2つの依存関係は今後もレンジ指定に戻さないこと。** バージョンを上げる必要が出た場合は、`npm view react-native-reanimated@<version> peerDependencies` で要求される worklets バージョンを確認し、両方を新しい組み合わせに完全固定してから実機でビルド確認する。

## 司令塔AI（複数プロダクト横断監督）— 保留中

- 構想：建設・法律相談・受験（・将来の証券・物販）を横断的にチェックするAI（Claude Agent SDK / Managed Agentsの`multiagent`構成を想定）。
- コスト：固定費ではなくAnthropic APIの従量課金。1日1回程度の軽いチェックなら月数千円規模、頻度や読ませる情報量が増えると数万円規模になり得る。
- **2026/6/16判断**：収益化（受験アプリのRevenueCat課金）が先に必要なため、司令塔AIの構築は保留。受験アプリの課金が稼働してから着手する。

## 収益化アイデア（バックログ・未着手）

ahiruのRevenueCat課金が最優先のため、以下は着手前のアイデア段階。優先順位は決めていない。

- FX口座開設アフィリエイト：ASP（A8.net／afb／バリューコマース／アクセストレード）経由でGMOクリック証券等と提携。成果報酬は口座開設1件あたり1万円程度（クリック課金ではなく成果報酬型）。金融商品取引法上、利益を保証する表現は不可。
  - **`miyabi-securities-app`（Zaibase証券・AI金融分析ダッシュボード）との組み合わせが本命**：AI予測を見たユーザーがそのままFX口座開設に流れる導線を作れる。ただし「このAIが勝つと言っている／だから今すぐ口座開設」のような誘導は、教育目的の免責と矛盾し、投資助言業の規制（金融商品取引法）に抵触するリスクがあるため、予測コンテンツとアフィリエイト導線は明確に分離して表現する必要がある。
- `miyabi-securities-app`の「新興テーマ株スキャナー」の既存「AI」テーマに、AI開発の地政学・輸出規制リスク（例：2026/6、米政府がClaude Mythos 5/Fable 5を輸出管理対象に指定し外国籍ユーザーのアクセス停止を命令、Anthropicが国籍を実時間で区別できず全世界向けに両モデル提供を一時停止。日経報道）をニュースセンチメント要因として追加する案。これは別リポジトリ（Cursor担当）側の機能追加であり、ここでは実装しない。下流の経済・市場への影響（日本経済への影響、中国AIの台頭など）は未確定の推測であり、確定事実として予測ロジックの前提にしないこと。
- ahiru：参考書・問題集のAmazonアソシエイトリンク設置
- kensetsu：保険・リフォーム業者の紹介料
- horitsu：弁護士紹介料（既存ルールの通り、弁護士法72条の確認が取れるまで保留）

## メール・クレーム対応

- `info@zaibase.group` → Cloudflare Email Routing経由の転送設定は**実際には無効（2026/6/16確認：Status Disabled、DNS records Not configured）**。前回記載の「2026/6/15設定・完了」は誤り。`info@zaibase.group` は転送ではなく独立したGoogleアカウント（自分自身の受信トレイ）として運用されている。
- Claude APIを使った自動返信：`info@zaibase.group` 宛の受信メール全てに「内容を確認のうえ3営業日以内に返信」とAI自動返信する仕組みが存在（場所はこのリポジトリ内ではなく外部・場所不明。クレーム/問い合わせ対応のため意図的に設置されたもので、停止しない）。2026/6/16、この自動返信がRevenueCatのチケットシステムと反応し合い、Ticket #79123〜#79133まで増殖するメールループが発生（実害なし、自然に停止）。**既知の課題**：no-reply/サポートチケット系の自動送信メール（RevenueCat等）にも無差別に自動返信してしまう。再発防止のため、`info@zaibase.group`のGmailフィルタで自動送信元（revenuecat.com等）を受信トレイスキップにする対策が未着手。
- クレーム対応は現在1人運営のため、コンテンツモデレーションを自動化済み

## 法律相談 招待・アクセスコード

### Firebase版 管理者パネル（弁護士向け）
- コード：`ZB-LEGAL-BF97-0773-FFB1`
- 用途：法律相談（Firebase版 https://zaibase-horitsu.web.app）の管理者パネルへのアクセス
- 共有方法：`info@zaibase.group` から弁護士へメール送付

### Cloud Run版 クローズドβ入場コード
- コード：`miyabi-beta-2026`
- 用途：本番URL（https://zaibase-legal-k56gkm3vfa-an.a.run.app）のβゲート突破。`BETA_MODE=1` 時に有効。`/beta.html` でコード入力→Cookie保存で以降はスキップ
- 変更方法：Cloud Run版リポジトリの `.env` / `deploy/.env.cloudrun.yaml` の `BETA_ACCESS_CODE` を書き換えて再デプロイ（Cursor担当）。`BETA_MODE=0` にすると一般公開（コード不要）

## デザイン（青空色パレット）

```
Primary:  #0EA5E9
Dark:     #0369A1
Mid:      #0284C7
Light:    #E0F2FE
Pale:     #F0F9FF
Muted:    #64748B
```

- PC背景：東京スカイラインSVG（base64埋め込み）
- モバイル最大幅：480px、border-radius: 14px
- フォント：-apple-system, BlinkMacSystemFont, 'Hiragino Sans'

## Cloud Functions ルール

- すべて v2（`firebase-functions/v2/https`）
- すべての onCall に `enforceAppCheck: true`（**ahiru は例外**：クライアントの App Check が Web のみ実装でネイティブ未対応のため、iOS で全 AI 機能が弾かれる。ahiru/functions は `enforceAppCheck` を外し、Firebase Auth 必須＋Firestore の利用回数制限で担保している。ネイティブ App Check を実装したら戻すこと）
- region: `"asia-northeast1"`
- Secrets は `defineSecret()` で定義

## 事業者情報

- 運営：南條 雅哉 / Zaibase Group
- 住所：〒543-0071 大阪府大阪市天王寺区筆ヶ崎町6-12-1701
- メール：info@zaibase.group（`zaibase.jp` は未取得のため使用不可）
- 電話：請求があった場合に遅滞なく開示（特定商取引法対応）

## Firebase プロジェクト

| プロダクト | Firebase プロジェクト | URL |
|---|---|---|
| Zaibase建設 | zaisai-share | https://zaisai-share.web.app/app |
| Zaibase法律相談 | zaibase-horitsu | https://zaibase-horitsu.web.app |
| 中学受験対策 ahiru | zaibase-exam | https://exam.zaibase.group |

## Stripe

- アカウント名：Zaibase Group（MIYABI WOR / acct_1TEhbMJtbSkZ2zlG）
- 建設プラン：starter ¥1,980 / pro ¥5,480 / team ¥14,800（月額）
- 法律相談：standard ¥2,980 / pro ¥7,800（月額）
- Zaibase建設プロプラン会員割引：法律相談が¥1,000引き

## 現在の開発ブランチ

`claude/water-soil-pollution-compliance-bekue1`

## よく使うコマンド

```bash
# 建設デプロイ
cd kensetsu && firebase deploy --only hosting

# 法律相談デプロイ
cd horitsu && firebase deploy

# Stripe Secret設定
firebase functions:secrets:set STRIPE_SECRET_KEY
firebase functions:secrets:set HORITSU_STRIPE_SECRET_KEY

# 受験(ahiru) ローカル起動・Web版ビルド
cd ahiru && npm install && npm run web
cd ahiru && npx expo export --platform web
```

## SEO・Search Console 状況（2026/6/21時点）

| サイト | プロパティ種別 | サイトマップ | 状態 |
|---|---|---|---|
| `zaibase.group` | ドメインプロパティ | `https://www.zaibase.group/sitemap.xml` | 成功・8ページ検出済み |
| `zaisai-share.web.app` | URLプレフィックス | `sitemap.xml` | 送信済み・数時間〜1日で「成功」に変わる見込み |

- Cloudflare TXT レコードは所有権維持のため削除しない
- `/undefinedsitemap.xmlsitemap.xml`（誤送信）は放置でOK（無害）
- **次のTODO**：Bing Webmaster Tools への登録（`zaibase.group` と `zaisai-share.web.app` の両方）。Google Search Console と同期する方法で所有権確認を自動完了できる。Yahoo 検索は Bing データを使うため、Bing 登録で Yahoo にも反映される。Cursor が対応中。

## 公開範囲（2026/6/21時点）

| プロダクト | 公開状態 | 備考 |
|---|---|---|
| Zaibase建設 `zaisai-share.web.app` | **公開中** | メインプロダクト |
| 会社サイト `zaibase.group` | **公開中（建設のみ案内）** | 法律・証券は「準備中」カード表示・リンクなし |
| Zaibase法律相談 | 非公開（アクセスコード制） | `/law` は直接URLのみアクセス可 |
| ahiru 受験 `exam.zaibase.group` | **公開中** | RevenueCat課金は未設定（プレースホルダー） |
| Zaibase証券 | 未公開 | 別リポジトリ・準備中 |
